# Decisions log

| Date | Decision | Alternatives | Reason |
| --- | --- | --- | --- |
| 2026-07-20 | Local-first compute: all development, exploration, and training on the local machine; Azure ML used to demonstrate the cloud workflow (one command job, versioned data assets, endpoint deploy→demo→teardown) | Cloud-first with a compute instance for daily work | Data fits in memory locally; faster iteration; near-zero cost; Azure knowledge still demonstrated where it counts |
| 2026-07-20 | uv for environment and package management (committed uv.lock, .python-version 3.11); pipelines/environment.yml is a conda-format manifest used only to build the Azure job image, pinned from uv.lock before submission | conda everywhere; pip + requirements.txt | User's standard tooling; lockfile reproducibility; no non-Python binary deps that would require conda; conda YAML is just Azure's manifest format |
| 2026-07-20 | ruff for lint + format (dev dependency group) with a minimal GitHub Actions CI (ruff check, format check, pytest); no CD to Azure | flake8/black/isort stack; CD pipeline with service principal | One fast tool; CI as engineering signal; CD would need repo secrets and endpoints are torn down by design |
| 2026-07-20 | One-time Azure infra provisioned via portal/CLI and recorded as commands in docs/azure_setup.md; ML-plane assets (data, environment, jobs, endpoints) stay code-first in pipelines/ | Terraform/Bicep IaC; undocumented portal clicks | IaC is out of scope for a DS portfolio; recorded commands keep reproducibility; code-first ML assets are the competency being demonstrated |
| 2026-07-21 | Markets: Thessaloniki, Athens, Crete (latest snapshots). Per city: detailed listings.csv.gz, reviews.csv.gz, calendar.csv.gz, neighbourhoods.csv | Summary listings/reviews files; a non-Greek third market | Query groups require `accommodates` (detailed file only); NLP requires review text; same-country regime removes cross-country confounds while spanning big-urban / mid-urban / seasonal-island markets; Thessaloniki links to prior host-scale work |
| 2026-07-21 | Excluded from the feature pool up front: `estimated_occupancy_l365d`, `estimated_revenue_l365d` (Inside Airbnb's own modeled occupancy), `availability_30/60/90/365` | Use them as features | They are derived from the same calendar/review signals as the label — quasi-circular; estimated_occupancy may appear in label *validation* only, caveated as review-derived rather than independent |
| 2026-07-24 | Label window = the 90 days **after** each city's snapshot date T (forward), replacing the build guide's "trailing" wording | Trailing window; review-derived occupancy estimates | The calendar file only covers T→T+365 — a trailing window is impossible from one snapshot; July–Sept is peak season (blocked≈booked most credible); far-future under-booking bias named in validation |
| 2026-07-24 | Preprocessing is lossless (PII removal, typing, dedup only); all row exclusion lives in `filters.py`, applied at label-build time with identical thresholds per city and reported counts | Bake filters/outlier removal into preprocessing (previous project's approach) | Filters stay revisable without re-processing; rank-based outlier cuts aren't portable across cities; a ranker must rank odd listings too — price outliers are a grading concern, not deletion |
| 2026-07-24 | Anonymization: drop host/reviewer PII + URLs; salted SHA-256 IDs (12 hex) applied consistently to listings/calendar/reviews; `license` → status + hash; `host_location` → `host_is_local`; listing `name` kept raw; coordinates kept unrounded; full contract in docs/data_pipeline_design.md | 6-hex unsalted MD5 (previous project); anonymized names; rounded coordinates | 6 hex collides at ~40k listings (dozens expected); unsalted hashes of public IDs are dictionary-reversible; Airbnb pre-jitters coordinates 0–150 m so rounding only destroys signal; titles are property marketing copy, not host PII |
| 2026-07-24 | Processed layer = one parquet per entity concatenated across cities with `city` + `snapshot_date` columns (schema equality asserted first); raw stays per-city/per-snapshot with a SHA-256 manifest, registration kept out of download.py | Per-city processed files; download script that also registers assets | One feature/training pipeline instead of three; per-row T prevents temporal misalignment; download must work offline from Azure — registration is a one-time recorded-command act per snapshot |
| 2026-07-24 | Azure assets: raw (per city/snapshot) + assembled feature table only; preprocessing stays local pure functions chained by one orchestrator — no per-function Azure ML components | Register intermediates; component-per-step cloud pipeline | Middle layers demonstrate nothing extra; per-function components are YAML ceremony at this scale; optional 2-step pipeline (prep→train) at Phase 3 if the pattern is wanted |
| 2026-07-24 | Dependencies: added `pyarrow` (parquet) and `scipy` (direct import in eda.py); rejected `squarify`, `geopy`, `requests` | Keep transitive-only scipy; add viz/geo deps | Direct imports must be declared; treemap dropped from EDA; vectorized haversine beats per-row geodesic; stdlib download suffices |
| 2026-07-25 | docs/data_dictionary.md is the single source of truth for raw-data semantics; the two research reports (data_assumptions.md v4.3, data_assumptions_2.md v4.7-inferred) are deleted, every checkable claim in them having been verified against our own snapshots | Keep both reports with "superseded" banners; keep them untouched | The v4.7 dictionary tab Inside Airbnb publishes is an empty placeholder, so 14 columns had no official definition and the second report's own caveat calls its definitions "strong hypotheses to validate against your data, not facts" — three docs, two of them wrong in places, is worse than one measured doc; git history retains them |
| 2026-07-25 | T (label anchor) = each listing's own `min(calendar.date)`, not the city snapshot date | Fixed per-city T; T = `last_scraped` | Scrape dates spread over up to four days *inside* a city (Crete's modal `last_scraped` is 06-30, not the 06-29 in the folder name); the per-listing anchor reproduces `availability_90` for 99.96–99.99 % of listings vs a shifted window otherwise, and the calendar carries a contiguous forward year per listing (365 days for all but 8 listings, which run 366-367), so a fixed 90-day window is always fully covered |
| 2026-07-25 | `price_quote_checkin_date` and `price_quote_checkout_date` added to `LABEL_ADJACENT_COLUMNS`; `price_quote_raw`, `price_quote_total_price`, `price_quote_price_per_night` dropped; `availability_eoy` blocklisted | Keep the price_quote family as Phase 1 core (the previous plan); drop the quote dates entirely | The quote check-in date is the listing's *first available calendar date* (91.5/86.5/68.5 % exact; Spearman 0.56–0.67 vs blocked-fraction-90) — a direct read of the label, because the scraper walks forward until a quote succeeds. Kept-but-blocklisted so notebook 02 can demonstrate the leak; raw JSON restates the dates and costs ~1 KB/row; the other two are exact functions of `price` |
| 2026-07-25 | `price` is imputed from the `city × neighbourhood × room_type × accommodates` median; a price-missingness indicator is never a feature | Pass NaN to LightGBM (native missing handling); drop price-null rows | `price` now equals the dated quote's per-night figure, so its nullity tracks availability: 84/15/68 % of price-null rows have `availability_90 == 0`. NaN is not neutral — LightGBM would split straight on the leak. Dropping instead would delete the highest-demand listings systematically (188/47/1,231 rows survive the inactive filter and need a price) |
| 2026-07-25 | Six planned features dropped as 100 % empty (`host_since`, `host_response_time`, `host_response_rate`, `host_acceptance_rate`, `host_total_listings_count`, `instant_bookable`), plus `has_availability` (constant); `hosts_time_as_*` and `host_listings_count` reversed from drop to keep | Impute the empty columns; keep the original v4.3-based column plan | 13 columns are 100 % null in all three cities. `hosts_time_as_*` become the *only* tenure signal, and user tenure ≠ host tenure (years agree in 62–77 % of rows); `host_listings_count` was slated for dropping in favour of two counts, one of which is empty |
| 2026-07-25 | `number_of_reviews_ly` admitted as a feature (with `ltm − ly` as a momentum signal) | Exclude as review-derived, like the other calculated fields | Verified to be exactly the calendar-2025 review count (100.0 % row-for-row match against reviews.csv, all three cities) — entirely before every T, so it is leakage-free, unlike the calendar- and occupancy-model-derived columns |
| 2026-07-27 | Filesystem layout centralised in `src/rental_ranking/data/paths.py` (`RAW_DIR`/`PROCESSED_DIR`), project root resolved as `$RENTAL_RANKING_ROOT` → nearest ancestor with `pyproject.toml` → cwd | `Path(__file__).parents[3]` in download.py (previous form); a repo-wide config.yaml | The fixed index encodes this module's depth in the tree and breaks on Azure, where `train_job.yml` uploads `code: ../src` and there is no `pyproject.toml` to find; paths are structural facts, so typed Python constants beat YAML (import-time failure, IDE navigation, `Path` objects). Hyperparameters stay out of a config file too — Azure job YAML already carries them, and a second copy would drift |
| 2026-07-27 | Anonymization invariants: hashing preserves nulls; IDs are canonicalised before hashing (integral floats collapse to ints); a missing/blank salt raises instead of defaulting; the derived-then-dropped `license` becomes `license_hash` | Hash NaN like any other value; hash `str(value)` directly; fall back to an empty salt | A hashed null gives every unlicensed listing one shared digest — inventing a single "operator" holding hundreds of properties and poisoning the duplicate-licence signal. One null widens an int64 ID column to float64, and `str(12345.0)` hashes differently, turning the calendar↔listings join into a silent zero-match. An unsalted digest of a public enumerable ID is dictionary-reversible while still *looking* correctly anonymized, so this is the only catchable point |
| 2026-07-27 | `clean.py` conventions: within-entity checks live in the entity's clean function, cross-entity checks in the orchestrator/notebook; every check warns once with a count and keeps the row (dedup on `id` is the sole exception); bounding boxes are generous administrative bounds in a `BOUNDING_BOXES` dict, not fitted to the observed extent | Raise on violation; per-row prints; boxes tightened to the data; scalar helpers called via `.apply` | A ranker must rank odd listings too, so preprocessing reports and keeps — exclusion stays in `filters.py`. Boxes fitted to the observed extent flagged 24 legitimate Crete and 1 Thessaloniki listing, which trains you to ignore the channel real problems arrive on; these are sized to catch swapped lat/long or a wrong-city merge. Series-in/Series-out throughout: the boundary cases that break pandas code (NaN, dtype coercion) only exist at column level, so scalar helpers are untestable where it counts |
| 2026-07-27 | `bathrooms_text` leads and `bathrooms` is the fallback; "Half-bath"/"Shared half-bath"/"Private half-bath" parse to 0.5; `bathrooms_shared` is null (not False) where the text is missing | Prefer the numeric `bathrooms`; swallow unparseable text in an `except`; default shared to False | `bathrooms_text` is the near-complete field (7 nulls in Athens vs 711). Leading with it cuts nulls 539→1 / 711→3 / 3,238→3 across the three cities. The half-bath forms carry no digit, so `float(text.split()[0])` raises on 23 Athens rows and an `except` would convert a known 0.5 into a missing value; absence of the word "shared" in a missing string is not evidence of a private bath |
| 2026-07-27 | No label anchor is computed in `clean.py`; `scrape_date` is carried per row and T is resolved in Phase 1 from `min(calendar.date)` per listing | Hardcoded per-city T dict; `scrape_date.max()` per city | The folder's release date is not the scrape date — Crete's folder says 06-29 but 16,446 of 27,333 listings were scraped 06-30 and 1,369 on 07-03, so a hardcoded dict is wrong for ~85 % of the market. `scrape_date.max()` collapses a per-listing fact to a per-city scalar and picks the latest, shifting the window forward for early-scraped listings. A listings frame cannot see the calendar, so it must not pretend to know T |
| 2026-07-30 | `build.py` is the sole orchestrator and the only module that writes `data/processed/`; the two cross-frame checks (schema equality before concat, join integrity after hashing) live here, not in the entity transforms | Chain the steps in an Azure ML pipeline; put the checks in `clean.py` | The contract registers only raw and the Phase 2 feature table — the processed parquet stays local, so there is no cloud step to hang the chaining from, and shipping 17M calendar rows to Blob to rebuild a local artifact demonstrates nothing. Orchestration YAML is a deployment artifact, not a program: logic inside it cannot be run locally, unit-tested, or debugged without a cloud round-trip. A Phase 3 `prep → train` pipeline calls `python -m rental_ranking.data.build` and holds no logic. The two checks need more than one frame, which no entity transform has |
| 2026-07-30 | Join integrity is verified with an **absolute** per-city resolution check plus a cross-city disjointness check (summed set sizes vs size of union), not by comparing per-city intersections against the post-concat intersection | The relational form: sum of per-city intersection sizes == intersection size after concat | The relational check is invariant-preserving, not correctness-establishing: if the salt were wrong for an entity in *every* city, per-city intersections are all empty, the sum is 0, the total is 0, and 0 == 0 passes while nothing joins. The absolute check catches exactly that case (verified: a deliberately mis-salted calendar reports 0.00% resolution and raises). Disjointness stated as `sum(sizes) == len(union)` covers the cross-city half the relational form was reaching for, without materialising intersections |
| 2026-08-01 | Filter rule 2 ("`first_review` inside the label window") dropped: `filters.py` implements two rules, not three | Replace it with a forward-window analogue (thin history: first review within ~30 days of T); keep it as a documented no-op guard | Measured 0 rows removed in all three cities, and it cannot be otherwise — the reviews file ends at the scrape date and T ≈ the scrape date, so `first_review > T` is impossible by construction under a forward window. The rule was inherited from the trailing-window design abandoned 2026-07-24; the contract always required filters to be re-derived rather than ported, and this is that re-derivation. The thin-history cohort it was reaching for (59 / 202 / 518 listings) is deliberately kept — it is the cold-start population Phase 2 flags with `has_reviews` and Phase 5 studies for fairness, so filtering it would delete the subject of later phases |
| 2026-08-03 | Label validation uses a **same-season-last-year** review window (reviews in `[T-365, T-365+90)`, anchored per listing), reported per city and decomposed by listing-age cohort; the claim made for the label is narrowed accordingly | Trailing window from T (the first attempt); the shipped `reviews_per_month` / `number_of_reviews_ltm` alone; a pooled cross-city correlation | A trailing window from T lands in May-June shoulder season while the label window is July-September peak, so it compares different demand regimes; the same-season window is still entirely pre-T, raises Thessaloniki 0.083 -> 0.124 against a trailing 40-day window, and at equal 90-day width (where season is the only difference) 0.061 -> 0.124 while having *more* empty windows in two of three cities, so the gain is seasonal alignment rather than extra data (corrected 2026-08-04; an earlier version of this row attributed it to zero-inflation, comparing mismatched populations). Extending it to 104/110/120 days for review-posting lag was tested and does not help (Thessaloniki degrades monotonically). Pooling is wrong because the between-city gradient runs opposite to the within-city one (Crete is the most blocked market and the thinnest-reviewed), so pooled rho sits below two of three cities. Most importantly, the correlation runs through **listing establishment**: mean blocked fraction is monotone in listing age in all three cities and restricting to established listings collapses rho to 0.034 / 0.151 / 0.065, which is why `reviews_per_month` — the one tenure-normalised signal — was weakest all along |
| 2026-08-03 | The label is characterised as a **two-stage signal** — establishment gets a listing into contention, review *quality* orders it within the established cohort — and `rating_shrunk` (empirical-Bayes rating shrunk toward the leave-one-out city mean, k~20) is adopted as the headline review feature | Report the establishment effect as a bare confound; use raw `review_scores_rating`; gate at >=10 reviews and discard the rest | Within the established cohort, rating tercile orders mean blocked fraction 0.275/0.371/0.442 (Thessaloniki), 0.383/0.429/0.470 (Athens), 0.606/0.686/0.693 (Crete), and Thessaloniki low-rated established listings (0.275) are indistinguishable from brand-new ones (0.277) — a year of tenure buys nothing without reviews to back it. The city ordering inverts between quality (0.255/0.136/0.177) and volume (0.084/0.172/0.128), which rescues Thessaloniki, the city that fails every volume-based validator. Crete only looked non-monotone because its top rating band had a median of 6 reviews vs 37 in the middle — instrument noise. Shrinkage beats both raw rating and the >=10 gate in every city (0.201->0.235, 0.075->0.151, 0.124->0.195) while keeping the whole population, because it encodes rating reliability continuously rather than discarding rows. Rating is also a cleaner instrument than review volume: review counts are mechanically produced by the bookings the label measures, ratings are not |
| 2026-08-03 | The price-imputation key stays **structural** (city, neighbourhood, room type, capacity) and never **behavioural** (cohort, listing age, `rating_shrunk`, `reviews_same_season_ly`); grading lives in `label.py` and takes partition *column names*, so it never imports `features/price.py` | Add cohort/establishment as a fifth strata key; impute with a model over the review features; put grading in `price.py` alongside the tiers | Imputed price feeds the price tier, which feeds the grading partition — so conditioning imputation on establishment (the label dominant driver) would assign the grading partition using the thing that predicts the grade. Not temporal leakage (all pre-T) but the tier stops being a price tier. It also makes imputation worse at its own job: adding `cohort` takes strata 1,135 -> 2,373, singletons 246 -> 649, and rows with no median in their own stratum 14 -> 26. And it buys ~10 % — never-reviewed listings sit at 1.10-1.13x their stratum median vs 0.97-0.99x for established — while the rows needing imputation are mostly established anyway (Crete 1,111 vs 292). The same columns remain legitimate as model features; only the imputation output feeds the partition that defines the target |
| 2026-08-03 | Quantile tie handling in grading is justified by the label 91-value grid, not by the 0/1 spikes; the partition rule (rare `room_type` collapse, minimum size, city x room_type -> city fallback) is explicit and test-pinned | Treat the spikes as the tie problem, as an earlier draft of NEXT_STEPS did; leave the partition rule implicit in whatever `qcut` does | Measured: under rank-then-cut into 5 grades, 2,551 listings (5.6 %, consistent at 5.4/5.6/5.7 % across cities) have a label value mapping to more than one grade inside their own partition — but only 0.3 % at 1.0 and 1.3 % at 0.0. The atoms sit at the extremes and fall wholly inside quintile 1 or 5, so a boundary rarely lands in them; the arbitrary splits are interior, where 91 discrete values mean many listings share a value straddling a cut point. The partition rule is not optional: there are only 33 partitions, 9 under 25 rows and 3 of size 1 (Thessaloniki Shared room is 3 listings across 3 price terciles), and Crete Shared room/high has 6 rows across 2 distinct label values |
| 2026-08-04 | Relevance grades use **scheme B — reserve the atoms, quantile the interior**: `blocked_fraction_90 == 0.0` -> grade 0, `== 1.0` -> grade 4, interior quantiled into grades 1-3 within `city x room_type x price tier` | Pure quantile grading (rank-then-cut into 5 within partition); fixed global cut points on `blocked_days`, equal-width or with the edges pulled in | Measured across all four. B halves the arbitrary-grade problem (rows whose label value maps to more than one grade in their own partition: 5.6 % -> 2.9 %) and cuts cold-start burial fivefold (never-reviewed listings in grade 0: 40.6 % -> 8.1 %, so 92 % of them land in the contention range). Fixed cut points eliminate arbitrary grades entirely but destroy cross-city comparability — under equal-width cuts grade 0 holds 42.2 % of Thessaloniki against 11.4 % of Crete, because the median blocked fraction is 0.256 there and 0.644 in Crete; pulling the edges in moves that ~5 points against a 31-point gap, and per-city thresholds are quantiles with extra steps. Cost of B, accepted and to be monitored: zero-variance query groups 17 -> 33 of 517 and mean distinct grades per group 4.12 -> 3.73, which contribute no LambdaMART gradient. Grade shares become 3.3 / 30.6 / 30.6 / 30.6 / 5.0 rather than a forced 20 % each — a ~5 % top class is realistic for a relevance scale |
| 2026-08-04 | Cold-start listings get `has_reviews` as a plain feature; a boolean conjoining a pre-T attribute with a label condition is banned outright | A flag for "zero reviews AND `blocked_fraction_90 == 0`" to tell the model that an unproven listing low blocked fraction means something different; assigning those rows a neutral grade | The proposed flag is not a mitigation, it is the target: measured, it implies `label == 0` exactly for every one of the 590 rows it marks, so the model would score perfectly on them and learn nothing. It would not look wrong in training either — a suspiciously strong feature and a validation score that collapses at serving time, when the forward calendar is unknown. Same shape as the already-banned "has price" flag, but worse: price missingness *tracks* the label, this one **is** the label. `has_reviews` alone carries the signal and is entirely pre-T (mean blocked fraction 0.214 / 0.243 / 0.418 for zero-review listings vs 0.338 / 0.398 / 0.627 for the rest), and the interaction is what trees do — give LightGBM the ingredient and it learns a different mapping in each branch. Assigning a neutral grade was rejected as fabricating a target; the 590 rows are carried and named as a limitation instead |
| 2026-08-05 | Phase split for the capacity/group question: a capacity **tier** is a listing attribute owned by Phase 1, a query **group** is an assembly owned by Phase 2; both live in `features/groups.py`. Tier bounds 1-2 / 3-4 / 5-7 / 8+ | Leave `accommodates` raw in the group key; five tiers (7-8 / 9+); three tiers (5+); keep the whole choice in Phase 2 as BUILD_GUIDE said, or the whole choice in Phase 1 as the contract said | The two roadmap docs disagreed — the contract said "the choice belongs to Phase 1", BUILD_GUIDE defines query groups in Phase 2 — and the split resolves it on a real dependency: price imputation consumes the tier as a cascade rung (it fills 4 of the 14 rows with no priced peer in their own stratum), so Phase 1 cannot defer the attribute, while group assembly and the minimum-size rule genuinely belong with the rest of Phase 2. On the bounds: `accommodates` takes 16 distinct values (1-16) with 87 % of listings at <= 6, so raw values fragment the key to 1,126 groups with 237 singletons and a median group of 6; four tiers give ~515 / ~65 and a median of 15. Between the two four-tier cuts the numbers are a wash (5-6/7+ gives 517/64, 5-7/8+ gives 512/66, identical imputation cascade fills), so semantics decided: capacity clusters on even values because beds come in pairs (5 = 9.1 %, 6 = 11.7 %, 7 = 2.7 %, 8 = 4.3 %), so `8+` starts the top tier on a party size people actually book for rather than on a near-empty value. Five tiers cost 62 more groups and 13 more singletons for no gain; three tiers reach 432 / 44 but put 5-guest and 16-guest properties in one search intent |
| 2026-08-05 | Query groups use a **minimum size of 5 with a fallback that drops the neighbourhood dimension** (undersized groups rank against `city x room_type x capacity_tier`); **no threshold on `neighbourhood_cleansed` itself**, and no spatial merging of sparse neighbourhoods | Collapse neighbourhoods below n=30/50 into an `Other` bucket; drop undersized groups outright; hand-merge adjacent neighbourhoods from a map; minimum size 10 | Thresholding one factor cannot fix a *product*: a neighbourhood with 6,009 listings still yields singleton groups once crossed with room type x capacity. Measured — of the 284 listings whose group falls below 5 rows, only 43 sit in a neighbourhood under 50 listings while 186 are in neighbourhoods of 100+ (Chania 6,009, Heraklion 2,075). A neighbourhood threshold moves usable listings only 99.6 % -> 99.7 %; the group minimum with fallback reaches 394 groups, 5 singletons, median size 29 and 99.9 %, dropping nobody — 284 listings (0.6 %) simply rank against a broader set. An `Other` bucket would invent a pseudo-neighbourhood and break the premise a query group rests on, whereas dropping the dimension is a coherent claim. Hand-merging was rejected on measurement and on process: sub-50 neighbourhood centroids sit 0.6 km apart in Athens but 16.6 km median / 39.8 km max in Crete, so a merge manufactures incoherent groups in the city holding 85 of the fallbacks; and a hand-drawn mapping is unauditable and rots when Inside Airbnb redraws boundaries. If ever wanted, the automated form (nearest centroid by haversine, capped at ~2 km) is the reviewable one; the coordinates earn more as Phase 2 distance features over all 45,869 listings |
| 2026-08-14 | `filters.py` gains two rules from an audit of implausible listings: **`is_dormant`** (>= 99 % blocked across the **whole** calendar) and **`is_extreme_price`** (> 20x the listing own structural stratum median). Ranked population 45,869 -> 44,684 | Define dormancy over days 90-359 only, to keep the rule independent of the label window; leave dormant listings in and caveat them; remove price outliers by rank; do nothing | The audit began as a test of the intuition "it is Greece in summer, so blocked ~ booked" and found the opposite of reassurance: 1,503 listings are >= 99 % blocked for their entire forward year, carry review histories (median 9, so `is_inactive` never saw them), were last reviewed a median of 540 days before T against 43 for the ranked population, and 94 % of them sat at a label of exactly 1.0 — the top grade. They are withdrawn properties, not booked ones, and `minimum_nights > 30` is already filtered so nothing legitimate is blocked for twelve straight months. Removing them moved every headline number in the label favour: rho 0.124/0.225/0.194 -> 0.150/0.226/0.233, and within the established cohort 0.034/0.151/0.065 -> 0.097/0.153/0.135, so a real share of the "establishment confound" was dormancy. **The whole-calendar definition is deliberate**: the narrower days-90-359 construction was tested and flags 1,493 seasonal operators actively selling in the window, which in a Greek market is the core population — a seasonal listing is open during the summer by definition, so a whole-year rule cannot catch one (`test_seasonal_operator_is_kept` is named for that mistake). `is_extreme_price` is a data-error guard rather than an outlier cut: 25 rows, worst at 374x, nulls never flagged because price missingness tracks the label and is imputed |
| 2026-08-14 | Near-twin listings (same host, same point to 4 dp, same capacity) are handled by a **grouped train/test split** on `features.groups.cluster_id`, never by deduplication | Drop all but one listing per cluster (keeping the most-reviewed); keep them and accept the leakage; collapse them into a single averaged row | Measured across 1,923 clusters covering 15.6 / 16.2 / 9.4 % of each market: only 5.8 % share a calendar and 15.3 % share a review count, and the median within-cluster spread in review count is 7. Cluster members are being booked independently and have accumulated different histories — they are **distinct inventory**, one operator with several identical flats in a building or a small aparthotel, and 93 % of clusters are `Entire home/apt` rather than `Hotel room`. Dropping them would delete 12 % of real bookable supply and would systematically remove the commercial-operator population Phase 5 fairness pass is about. The leakage is real but is a *splitting* problem: median within-cluster label spread is 0.079, so twins on both sides of a random split let the model memorise the pair instead of ranking. A grouped split costs one `groups=` argument and loses no rows. `cluster_id` therefore lives in `features/groups.py` and is documented in both that module and `filters.py` as "for splitting, never for filtering" |
| 2026-08-16 | The grading partition is **`city x room_type`**; `price_tier` is dropped from it entirely, and with it the price tier as a Phase 1 artefact. A grading partition must be a **coarsening of the query-group key**, never a cross-cut of it | Keep `city x room_type x price tier` as decided 2026-08-04; add `capacity_tier` as a third dimension; grade within the query group itself; grade within `city` alone | The rule is provable and was then measured. If every listing in a query group sits in one partition cell, the grade is a monotone step function of the label inside that group, so the target can never contradict the label; if the partition cross-cuts the key, it can. Measured over 516 query groups: every coarsening (`city`, `city x room_type`, `city x room_type x capacity_tier`, `city x neighbourhood`, the group itself) produces **0 groups with an inversion**, while `city x room_type x price_tier` produces **145** and `city x price_tier` **157** — groups holding 39,801 rows in which some lower-demand listing outranks a higher-demand one in the target LambdaMART is trained on. Price had also failed to earn its place on the merits: its mean-label spread across tiers is 0.032/0.033/0.065, the smallest of every candidate dimension (neighbourhood 0.090/0.199/0.299, room_type 0.027/0.222/0.146, capacity_tier 0.065/0.067/0.114, city 0.273), Spearman(price, label) within city is 0.047-0.108 and collapses to -0.058..+0.109 within `Entire home/apt x capacity_tier` with the sign flipping by city, and the decile curve is non-monotone (Athens is U-shaped). Against `city x room_type` it moved 4.6 % of grades and improved zero-variance groups by one. `capacity_tier` passes the coarsening test and has a real monotone gradient, but adds nothing on top of `room_type` (zero-variance 29 -> 28, distinct grades per group 3.30 -> 3.30) while splitting 11 cells into 41 with 15 under 30 interior rows — left out as a free choice, not a principled one. Grade **shares are invariant** to the partition under scheme B (the atoms are global and the interior always splits into thirds), so only which listings get 1 vs 2 vs 3 changes. Consequence worth noting: price no longer feeds the target at any point, so the planned imputation sensitivity check is void and `features/price.py` needs no tier function |
| 2026-08-16 | Missing price is filled by the **median** of the structural cascade, not by a KNN or model-based imputer | `sklearn.impute.KNNImputer` over the listings frame; hand-rolled geographic KNN (haversine on lat/lon within `city x room_type x capacity_tier`); iterative/model imputation | Benchmarked leave-one-out on 8,000 rows that do have a price, median absolute error: `city` 39.6 %, `city x room_type` 39.5 %, `city x room_type x accommodates` 28.6 %, **the contract cascade 26.3 %**, geo-KNN k=5 **23.9 %**, k=10 24.0 %, k=25 25.7 %. Geographic KNN is genuinely better, by 2.4 points — and rejected anyway: the error floor is ~24 % either way (price is intrinsically hard to predict from structure), it would apply to 694 rows (1.6 %) of a feature correlating 0.047-0.108 with the label, and price no longer feeds the target at all after the partition decision above, so nothing downstream can detect the difference. Two specific hazards decided it: `KNNImputer` given the listings frame would silently use `number_of_reviews`, `availability_90` and `estimated_occupancy_l365d` as neighbour dimensions, which is every objection to a behavioural key applied invisibly; and it averages, which at skew 6.5 with a 9,243 maximum is the wrong statistic. The variant that won is not a fancier model but a **more structural** one (lat/lon only, stratified), so the finding is kept as write-up material rather than as code |
| 2026-08-16 | Relevance grades use **scheme E — reserve the zero atom only**: `blocked_fraction_90 == 0.0` -> grade 0, everything above it quartiled into grades 1-4 within `city x room_type`. Cuts land on the label's **value** (left-open, right-closed), never on `rank(method="first")`. Replaces scheme B (both atoms reserved, interior into 1-3), decided 2026-08-04 | Keep scheme B; scheme A pure quintiles; scheme F reserving only the 1.0 atom; rank-based cuts with `duplicates="drop"` | Re-derived on the post-audit population once the partition changed. Measured, **the entire cold-start benefit comes from the zero atom**: schemes B and E both put 8.0 % of never-reviewed listings in grade 0 against 38.6 % under plain quintiles, while scheme F (1.0 reserved, 0.0 pooled) reaches 43.9 % — worse than quintiles. Reserving 1.0 as well was therefore pure cost: it produced a 2.6 % top class present in only 41 % of the 516 query groups, against 73 % for E, and made a single blocked night out of ninety decide a grade boundary on a label where blocked is not booked. The dormancy filter also removed the reason B existed — every listing now at 1.0 carries reviews (132 / 372 / 664, median 18.5 / 12 / 8), so it earns the top quartile rather than needing a reserved seat. Final measurements from the shipped `assign_grades`: shares 3.4 / 24.6 / 24.1 / 24.4 / 23.4, mean label by grade 0.0 / 0.168 / 0.413 / 0.602 / 0.829, zero-variance query groups 27 of 516 falling to 10 of 399 under the Phase 2 min-5 fallback, mean distinct grades per multi-row group 3.91 -> 4.27, grade 4 reaching 72.9 % -> 86.0 % of groups. **Cutting on value rather than rank drops the tie problem from 4.2 % to exactly 0** — every listing sharing a label value now shares a grade, and the grade is provably non-decreasing in the label inside each cell; the cost is quartiles that are not exactly equal, because a boundary value takes its whole tie group into the lower bin. Two cells fall under the 30-row minimum (Athens and Crete Shared room) and are graded against the **whole city population**, not against each other — an earlier draft pooled the undersized rows together, which spread five listings across all four grades and defeated the minimum entirely; `test_undersized_cell_falls_back_to_the_coarser_partition` is named for that bug. Accepted and stated as a limitation: grade 0 is an absolute criterion while grades 1-4 are within-partition quantiles, so grade 0 holds 7.1 % of Thessaloniki against 2.0 % of Crete. Zero-review listings with a zero label are **kept** in grade 0 rather than given a neutral grade, which would fabricate a target |
| 2026-08-16 | The temporal design is stated as a **feature-provenance guarantee**, never as a train/test split in time; the 26 listings that breach it are **kept and named** rather than filtered | Exclude the 26 as a fifth filter rule; describe the design as a temporal split without qualification; say nothing and let the reader assume it is universal | The split holds exactly when `scrape_date <= T`, which is a countable property rather than an assumption, so it was counted: **26 of 44,684 listings (0.06 %)** — 5 Athens, 21 Crete, thirteen at one day and thirteen at two — had attributes recorded just inside their own label window, because the scrape ran across four calendar days in those two markets. Kept, because excluding 26 rows would mean a fifth rule in `filters.py`, and every rule there carries a stated threshold, an identical application across cities, and a reported count; a quiet drop of 26 rows earns none of that. The breach is **nominal rather than material and that is measured**: structural attributes (room type, capacity, neighbourhood) cannot change in two days, the one class that could is review-derived — the features §3 and §4 lean on — and **not one of the 26 has a review dated after its T**, so nothing actually moved. Their mean label of 0.527 against 0.482 overall is Crete being over-represented among them, not an effect of the gap. Separately and more importantly, **one snapshot cannot supply a split in time at all**, so the README and notebook 02 §7 state provenance and temporal validation as two different things rather than letting the first imply the second; the Phase 3 holdout is a grouped split on `cluster_id` and is a decision of its own |
| 2026-08-16 | Query groups take a **second fallback rung**: minimum size 5, dropping `neighbourhood_cleansed`, then `capacity_tier`. Supersedes the one-rung plan of 2026-08-05. Two implementation rules go with it: the fallback **pools the fallers among themselves** and never re-opens a settled group, and **every rung must retain the grading partition's columns**, checked at runtime | Keep the single rung; collapse rare `room_type`s into an `Other` bucket; drop the groups that stay under five; merge fallers into the healthy group that shares their coarser key | Measured on the ranked population — groups / singletons / under 5 / median: no minimum **516 / 71 / 153 / 14**, one rung **399 / 6 / 15 / 28**, two rungs **393 / 0 / 7 / 29**, rung usage 44,395 / 255 / 34. The second rung moves 34 listings (0.08 %) and is worth it for one reason: it is what reaches **zero singletons**, and a singleton is strictly worthless to LambdaMART — one document, no pair to compare, no gradient — while being invisible to every metric. The `Other` collapse was rejected again on the same argument that rejected a neighbourhood threshold: rare types are 307 listings (0.69 %) and **seven of the 15 groups still short after one rung involve the `8+` capacity tier rather than the room type**, so thresholding one factor cannot fix a *product*; the second rung beats the collapse outright (393 / 0 against 391 / 3) and invents no pseudo-category. Pooling rather than merging is what keeps the fallback from coarsening the population that is already fine: absorbing two fallers into a healthy six-row group would dissolve a working group to rescue a broken one, and each rung is re-tested against the minimum so a pool that is still short falls again. The partition check encodes the half of the coarsening guarantee that a constant cannot state — the guarantee is a relationship between two keys, so `query_group` compares every rung against `label.DEFAULT_PARTITION_COLS` and raises; verified on the real key, **no query group spans two `city × room_type` cells**. The terminal rung accepts what it is handed (7 groups under five rows) rather than dropping listings the filters chose to keep |
| 2026-08-17 | Amenities are encoded through **canonicalisation plus a 19-bucket concept map** (`features/amenities.py`), built label-blind from the vocabulary alone and pinned as a constant. The map asserts the **partition only** — bucket membership — and leaves every weight to the model. Rejected: a hand-built cardinal "convenience score" summed per listing; a per-string dictionary; frequency-ranked top-k flags as the *primary* encoding | Top-20/50/100 binary flags by frequency; a hand-weighted convenience score; IDF/rarity weighting; SVD or NMF components; amenity-profile clustering; feature hashing; text embeddings; mean-label (target) encoding per amenity | The vocabulary is **7,029 distinct strings over 44,684 listings, 93 % of them appearing in under 0.1 %** — an unbounded tail of subtype and brand variants, so one-hot is impossible and a per-string dictionary is brittle (it scores "Fast wifi - 500 Mbps" as zero because the string is not "Wifi"). Two measurements decided the rest. **Rarity is not value**: "Body soap" (49.5 %) is *rarer* than "Air conditioning" (70.6 %), and ranking the vocabulary by frequency puts "Hangers" (85.1 %) above air conditioning — so no frequency- or IDF-derived weighting can stand in for judgement, and the top-20 encoding would spend its entire budget on commodity items (Wifi, Hair dryer, Kitchen, Hot water, Hangers, Iron, Bed linens, Dishes) before reaching AC at rank 12. **The feed also splits single concepts across mutually exclusive subtypes**: air conditioning appears as four variants at 70.6 / 13.7 / 9.4 / 5.0 %, none of which answers "does this listing have AC" — the bucket answers it for 98.7 %. Against that, a *cardinal* score was rejected because it asks its author to defend fifty numbers while a partition asks only that "parking is a category", and a GBM learns weights over a dozen bucket counts far better than a human assigns them — **assert the partition, let the model assert the weights**. Target encoding is forbidden outright (leakage), which is precisely why a label-blind hand map is the only legitimate route for domain knowledge here; the corollary is that **the map may never be tuned against an evaluation score**, since a dictionary fitted by watching NDCG is target encoding performed through the author's eyes and no test would catch it. Measured coverage: **98.9 % of the 1.74 M amenity mentions and 99.0 % of distinct strings**, with 95 % of the unmapped remainder being deliberately excluded "available upon request" phrasings — a host who *can* find a crib is not a listing that has one. Rule order is load-bearing (a pool table is not a pool, a pool view is not a pool, an outdoor kitchen is not a kitchen appliance) and is pinned by `tests/test_amenities.py`. Consequence for the feature block: **8 of the 19 buckets are near-universal on presence** (connectivity_work 99.2 %, air_conditioning 98.7 %, kitchen 98.2 %, laundry 96.5 %, entertainment 95.4 %, basics 93.6 %, safety 92.3 %, access 90.1 %), so per-bucket **counts** carry the variance and per-bucket flags would not; the 11 that do discriminate on presence are fitness 6.7 %, beach_water 27.8 %, pool_spa 28.1 %, services 29.6 %, view 33.0 %, family 43.7 %, parking_street 44.0 %, parking_private 46.2 %, policy 65.6 %, outdoor_space 76.9 %, heating 78.9 %. The rejected encodings stay as **implemented alternatives behind one scheme parameter**, compared on validation NDCG in Phase 3 and never on test |
| 2026-08-17 | **`bedrooms` is not imputed** — 7.5 % null passes through to LightGBM as NaN. By extension nothing in `features/listing.py` is imputed; `price` remains the sole exception, and it is filled in Phase 1 for a reason that does not generalise | Median (or mean) fill; `sklearn.impute.KNNImputer`; a structural cascade like `price.py`'s; a fill plus a `bedrooms_missing` flag | The test that decides it is the one that mirrors how the model is trained: **within a query group, the mean label percentile of a null-`bedrooms` listing is 0.498 against 0.505** for the rest — a pairwise ranker compares only inside a group, so there is nothing to exploit. The per-city marginal gap that *is* visible (0.022 / 0.067 / 0.050) is **composition, not signal**: nulls concentrate in private rooms (**31.5 %** against 5.6 % for entire homes, 26.2 % shared, 11.7 % hotel), and `room_type` is part of the query-group key, so the confound is differenced out by the group itself. Conditioned on room type the gaps even flip sign (−0.031 / +0.036 / −0.073), the signature of noise. That is the structural contrast with `price`, whose missingness sat on a **mechanical** path from the label (no availability → no quote) with gaps of 0.027 / 0.148 / 0.117 all one way. Three further reasons: **null frequently means "not applicable"** rather than "unknown" — a private room has no bedroom count to report, so a fill invents an attribute; **NaN is the weaker assumption**, since LightGBM learns a split direction for missing (fitted) while any fill asserts a value (assumed); and **mean/median are wrong for a small-integer count** (1: 21,699, 2: 11,291, 3: 4,914 — a 1.6-bedroom listing cannot exist and lands between two real categories). KNN was already benchmarked and rejected for `price` *while winning* (23.9 % vs 26.3 %), and its decisive hazard applies verbatim here: `KNNImputer` handed the listings frame silently uses `number_of_reviews`, `availability_90` and `estimated_occupancy_l365d` as neighbour dimensions — blocklist columns entering a feature invisibly. Stakes are low either way: ρ(bedrooms, accommodates) = 0.834, and the modal bedroom count given `accommodates` is exact for 71.8 % of rows and within ±1 for 97.9 %. The column is **kept** regardless, because the ~28 % it does not pin is real product information (a 4-guest studio and a 4-guest two-bedroom are different listings). A fill-plus-flag was rejected outright: the flag is the "has price" pattern this project already banned |
| 2026-08-17 | **`review_momentum` is dropped.** `rating_shrunk` keeps **k = 20**, stated as a prior rather than a fitted value. Ages (`listing_age_days`, `days_since_last_review`) pass through **null** for the never-reviewed cohort | Keep it as the difference `ltm − ly`; keep it as a ratio `ltm / ly`; keep it scale-normalised as `(ltm − ly) / (ly + 5)`; a sentinel age for never-reviewed listings; fitting k against label correlation | Considered in three forms and none earns a column. **The difference does not respect scale**: `rho(|momentum|, number_of_reviews) = +0.589`, so its magnitude is mostly listing size — listings with 500+ reviews average −6.1 (sd 9.5) while those with 15–30 average +1.2 (sd 5.3), and the two are not comparable. Since review counts vary enormously *inside* a query group, that confound is live in the comparison unit rather than differenced out by it, and the earlier argument that "`number_of_reviews` is a feature so a tree conditions on size" was too generous: the correction is needed everywhere, not in a niche, and costs interaction depth throughout. **Normalising does not rescue it** — `(ltm − ly)/(ly + 5)` measures −0.040 / −0.018 / +0.034 within the established cohort against the difference's −0.050 / −0.024 / +0.029, both near zero and both sign-flipping across cities, the same pattern that rejected the price tier and the distance zone. A **ratio** was independently unusable: the windows overlap by six months (`ltm` runs 2025-07-03 to 2026-07-03, `ly` is calendar 2025, verified at 100 % against a recount), which cancels exactly in a difference but not at all in a ratio, and `number_of_reviews_ly` is 0 for **34.7 %** of listings and ≤ 2 for **50.6 %**. On **k = 20**: the empirical-Bayes optimum is within-listing variance over between-listing variance and the numerator is **unobservable here** — Inside Airbnb's reviews file carries only `listing_id, id, date, comments`, with no per-review score — so 20 is a convention. Selecting it belongs in the same validation comparison as the amenity scheme. Ages stay null for the reason `bedrooms` does: an age of 0 for a listing never reviewed is a value that looks like data, and `has_reviews` already names the cohort |
| 2026-08-17 | **No neighbourhood mean-label aggregate, at any scale.** `features/aggregates.py` builds leave-one-out aggregates of **price and listing count only**, plus the relative feature `price_vs_nbhd`. Leave-one-out is applied **unconditionally**, never only to small neighbourhoods | Build the LOO mean-label aggregate as BUILD_GUIDE prescribes; build it include-self; apply LOO only where n is small (say n < 30); use the LOO mean price instead of the median | BUILD_GUIDE calls including-self "the most common silent leak in this design" and prescribes leave-one-out as the fix. Measured against **this** group key, leave-one-out does not fix a label aggregate — **it creates the leak**. 365 of 393 query groups sit inside a single neighbourhood, so within a group the neighbourhood total `S` and size `n` are constants and the LOO mean is `S/(n-1) - x_i/(n-1)`: an exact affine, strictly **decreasing** function of the listing's own label. Measured within-group Spearman against the label: **exactly -1.000 in 100 %** of those groups. The include-self version fails in the mirror image and is merely useless — one distinct value per group, so it separates no pair. The leak's absolute size is small (within-group range median 0.002, max 0.058 against a full range of 0.521) and LightGBM's default 255-bin histogram would quantise the median case into about one bin — but the maximum spans roughly 28 bins, and it is largest exactly in the small neighbourhoods where a model overfits most, so that is protection by accident rather than by design. This **extends the roadmap's "never aggregate the label at query-group scale" one level up**: with this key the neighbourhood contains the group, so the same objection applies. On **unconditional LOO**: the include-self/LOO gap is exactly `(x_i - mu)/(n-1)`, decaying as `1/(n-1)` — about 3 % of a listing's deviation at n = 30, 0.1 % at n = 1,000, and 0.0004 on average here — so restricting it to small neighbourhoods would buy nothing while costing a threshold to justify, a branch to maintain, and a feature whose definition changes discontinuously at the threshold. On **median over mean**: the same skew argument as `price.py` (skew 6.5, `is_extreme_price` deliberately keeps everything under a 20x guard). Measured consequences: `nbhd_listings` and `nbhd_median_price` have within-group variance ratios of **0.015 and 0.034** — they are *conditioners* like `room_type` and `city`, not discriminators — while `price_vs_nbhd` reaches **1.147** against `price`'s 0.757. Within a group `price_vs_nbhd` is perfectly monotone in `price` (rho = 1.000), so it adds no within-group ordering; its value is that **one split transfers across neighbourhoods** where a split on raw price must be relearned in each. A lone neighbourhood (n = 1) yields **NaN**, never zero |
| 2026-08-17 | **Review sentiment is not a model feature.** Scope reduced to **demonstration only**, inside the Azure AI Language **F0 free tier** (5,000 text records/month), run once on a stratified sample with raw responses cached before any aggregation. No `torch`/`transformers` dependency is added. Decided **before any spend**, on measurement | Full local inference for all 37,391 reviewed listings (~2 h on the M4, but a permanent ~2-3 GB dependency in an image pinned from uv.lock); one paid Azure run (three figures against a EUR 25-30/month budget); sentiment for a targeted subset only — the 10 smallest query groups, or the top-N most-reviewed listings within popular groups; aspect/opinion mining as a feature block | Piloted locally at zero cost with an **ephemeral** environment (`uv run --with`, lockfile untouched): 80 **whole** query groups, 1,643 listings, 14,525 reviews scored with `cardiffnlp/twitter-xlm-roberta-base-sentiment` on MPS in ~4 minutes. Measured **within query groups**, which is the only comparison a pairwise ranker makes, mean rho against the label: `review_scores_value` **+0.128**, `review_scores_rating` +0.082, **sentiment +0.049**, and **sentiment with the rating partialled out +0.015** — indistinguishable from zero across 80 groups at SE ~ 0.025. **The ceiling test fails outright**: among the 463 listings at rating >= 4.95, sentiment genuinely varies (sd 0.117), so it does de-compress the ceiling the rating cannot — but rho against the label among them is **-0.026**. The mechanism works and the signal is absent. An earlier 405-listing pass suggested a residual of +0.102; that was small-sample noise measured pooled rather than within groups, which is why the second pilot sampled whole groups. **Aspect mining fails on coverage rather than on signal**: the aspects with usable coverage are exactly the ones Airbnb already ships as numeric sub-scores (clean 85.7 % of listings, location 80.7 %, median 2 mentions each), while the unrated aspects that could add something have a **median of zero mentions per listing** — noise 45.4 % of listings, bed 23.7 %, parking 17.8 %, wifi 17.5 %, air conditioning 13.1 %. A per-aspect feature would be null for most rows and built on a single sentence where it is not. The targeted-subset alternatives were rejected on construction: scoring only the top-N most-reviewed listings in a group makes "has sentiment" a proxy for review count, which is the label's dominant driver, so the model would rank on the **missingness** without ever reading the value — the banned "has price" flag pattern; scoring only the smallest groups covers ~30 listings of 44,684 and yields a two-regime model. Consequences: **no new dependency**, so the cloud image stays light, and `features/sentiment.py` carries the demo path rather than a feature block |
| 2026-08-17 | **Baselines frozen before any model exists**, with a **random-ranking control** added as a third. Definitions live in `train/baseline.py`, metrics in `evaluate/metrics.py`, and the numbers are pinned by `tests/test_baseline.py` — a log entry is a promise, a test is the mechanism. Recorded full-population NDCG@10: **reviews 0.6424** [0.6233, 0.6606], **price+rating 0.6218** [0.6015, 0.6417], **random ~0.540** (mean of 20 draws, sd 0.006) | Record the baselines after the Phase 3 split; use raw `review_scores_rating` in baseline B; z-scores instead of within-group percentiles; omit the random control; score degenerate groups as 1.0 the way LightGBM does | **The random control is the finding.** NDCG has a high floor — a random ordering already scores **~0.540** — averaged over 20 draws, because a single draw varies by sd 0.006 and four decimals from one would be false precision on the number every headline is read against — so the usable range is 0.540 to 1.0 and baseline A's 0.6424 traverses 22 % of it, not a 64 % one. Every model headline must be read against the random floor; reporting 0.68 as though the scale started at zero would overstate the result roughly threefold. **The recorded figures are full-population reference numbers, not held-out estimates** — the grouped split arrives in Phase 3, and comparing a full-population baseline against a test-set model would compare two different things; Phase 3 re-runs this same code on the test split alongside the model. Two construction choices in baseline B keep it **strong rather than convenient**, because a weak baseline flatters the model: **within-group percentiles** rather than z-scores (price has skew 6.5 and a 9,243 maximum, so a z-score is dominated by a handful of rows), and **`rating_shrunk` rather than raw `review_scores_rating`** (the raw score ties 32.7 % of reviewed listings at exactly 5.0 and is null for 16.3 %, so a baseline built on it would be crippled by ties and missingness, and beating a crippled baseline proves nothing). Baseline A leads B (0.6424 vs 0.6218), which is what Phase 1 predicted from an establishment-driven label, though their intervals overlap. **Degenerate groups are excluded, not scored 1.0**: 10 of 393 groups carry a single grade, where any permutation is perfect, and LightGBM's internal convention of scoring them 1.0 would inflate the mean for free. Metrics average **over groups, never over listings** — pooling would weight the 2,088-listing group a thousand times the 6-listing one — and ties break on row order (hashed-id, unrelated to the grade) rather than favourably. **NDCG@10 varies far more by group size than between baselines**: 0.807 for groups of 1-10, 0.606 for 11-50, 0.541 for 51-200, 0.624 for 201+, so the overall average mixes very different measurements and Phase 3 must report the stratification. Recall@10 is degenerate at both ends — 1.000 for groups of 10 or fewer by construction, 0.040 for the largest — so NDCG carries the headline and Recall is reported per band or not at all |
| 2026-08-17 | Azure data assets are declared in **YAML files under `pipelines/data/`**, not as inline `az ml data create` flags; four assets — three raw (one per city, version = snapshot date) and the feature table (version = build date). The processed layer is **not registered** | Inline CLI flags as previously recorded in azure_setup.md; a single combined raw asset; also registering `data/processed/`; hardcoding the git SHA in the feature-table YAML | The deciding fact is mechanical: `az ml data create` has **no `--tags` argument** in the installed extension, so the tags that carry traceability — `raw_versions`, `git`, shape — are expressible only in a YAML file. The inline commands recorded earlier could never have worked. Secondary: `path` resolves relative to the YAML rather than the working directory, the repo already specifies its environment and training job in YAML, and a committed file that is actually executed beats a commented-out command in a markdown file that was never run (the `az configure --defaults` line in azure_setup.md had exactly that fate, which is what produced the misleading `Resource group 'None'` error — the missing flag is `-w`). Raw stays split per city because Athens released 2026-06-28 against 06-29 for the other two, and one combined asset would have to discard that. Processed is a reproducible intermediate that nothing in the cloud consumes. `git` is injected via `--set` rather than written into the YAML because a stale hardcoded SHA is a *wrong* tag while a forgotten `--set` is merely a *missing* one, and only the former breaks the chain silently |
| 2026-08-18 | **The cluster/group conflict is resolved by option A: split on the connected components of the `cluster_id` x `query_group` bipartite graph** (`train/split.py::split_component`). Both constraints hold exactly — no near-twin cluster straddles the split, no query group is broken, no listing is dropped. Measured: **345 components** over 393 groups and 41,148 clusters; 305 hold one group, 32 hold two, 8 hold three; none crosses a city; largest holds 2,240 rows (5.0 %) | B: split on `cluster_id` and accept 88 partially evaluated groups (22 %); C: split on `query_group` and accept 253 listings (0.57 %) of twin leakage — the leak the BUILD_GUIDE names | The roadmap's ~5 % abort threshold is met at the boundary but reads worse than it is: **the lumpiness comes from the group-size skew, not the components**. The largest single query group is already 2,088 rows (4.7 %), and the top-five unit sizes go 2088/2042/1411/1160/1114 as groups against 2240/2135/1411/1305/1247 as components. Option A therefore costs ~7 % on the largest unit and 48 units of granularity, in exchange for both constraints exactly. B and C pay a real price to avoid a cost that is not there |
| 2026-08-18 | **Evaluation protocol: one sealed fold plus a four-fold development pool**, not a single 80/20 split with one validation set. `assign_folds` cuts the components into 5 equal folds; **fold 0 is sealed** and touched once; every selection decision — amenity scheme, hyperparameters, stopping iteration — is made by `dev_cv_splits` over folds 1–4. Headline is the sealed-fold number with baselines **recomputed on the sealed fold's groups** (not against the frozen population figures); the ~307-group development out-of-fold number is reported beside it as the precision estimate, explicitly labelled as the number the decisions were made on | The roadmap's 80/20 + one validation split; 5-fold CV as the headline with no sealed fold | Measured 2026-08-18: **baseline A minus baseline B is a constant** (neither is fitted, neither reads the label; population value 0.0207), yet read off five candidate 20 % test halves it reports **0.0151 / 0.0116 / 0.0487 / 0.0249 / 0.0048** — a tenfold swing on a quantity that does not vary. Per-group NDCG@10 has std 0.187, so at ~77 groups the 95 % half-width is ±0.041 on a level and ±0.037 paired, the same size as the effect the phase exists to detect. A single validation split of that size would select hyperparameters on noise. Four folds put ~307 groups behind every decision at no cost to the sealed half. Pure CV was rejected because it leaves no untouched holdout |
| 2026-08-18 | **Folds are stratified on city × group-size band and balanced on rows; `grade` is never read by the objective.** Bands are `<10 / 10-30 / 30-100 / 100-400 / 400+`. Components are placed largest-first into whichever fold leaves the population least imbalanced. Realised on the shipped table: row shares 0.1997–0.2002, per-fold groups 74–84, size-band shares within 1.6 pp, grade-4 share 21.5–24.5 % unforced | City-only stratification (what the roadmap asked for); adding grade to the objective; random assignment of components | Band matters more than city: per-group NDCG@10 for baseline A by band is **0.824 / 0.642 / 0.545 / 0.576 / 0.646**, a 0.28 spread that swamps anything a model will do, so a test half drawn heavy in small groups scores higher for *every* ranker in the table. Band is a pre-label structural property, so balancing on it is not conditioning on the target — **grade is not**, and is reported by `fold_balance` rather than optimised. Largest-first is required because the 2,240-row component must be placed while there is still room to compensate for it |
| 2026-08-18 | **The sealed fold is chosen by rule (index 0, seed 0), declared before any metric is computed on it.** The per-fold baseline spread recorded above was measured on a throwaway scratch assignment that `split.py` does not reproduce, so no fold identity carries over | Picking the fold after inspecting its baselines | Choosing "whichever fold the baseline is weakest on" is the silent version of tuning on test. The rule has to predate the numbers, and the scratch/production break is what makes that claim checkable |
| 2026-08-18 | **Per-city NDCG is reported from the development out-of-fold, not the sealed fold.** The sealed fold holds 38 / 30 / **4** groups for Athens / Crete / Thessaloniki; the dev pool holds 157 / 130 / 24. Both tables are printed, with `groups` visible as a column, but no per-city *claim* is made from the sealed fold | Reporting per-city from the sealed fold only; dropping Thessaloniki from the per-city table; merging Thessaloniki into a pooled "other" row | A group's NDCG is a **deterministic** function of its rows, so the 88 listings inside Thessaloniki's 4 sealed groups buy no precision — the uncertainty is entirely *between* groups (per-group sd 0.187) and there are 4 draws. 95 % half-widths: sealed ±0.059 / ±0.067 / **±0.183**, dev ±0.029 / ±0.032 / ±0.075. A ±0.183 interval on a 0.458-wide usable range covers "excellent" and "at the random floor" at once, and the percentile bootstrap at n=4 has only 35 distinct resamples so the interval is a step function, not an interval. 54 groups are needed for ±0.05, 14 for ±0.10 |
| 2026-08-18 | **Every report is sliced at `n > k` as well as overall, automatically, and the random floor is recomputed per slice.** `evaluate/report.py::comparison_table` emits `overall`, `n>k` and `n<=k` for every ranker with paired-bootstrap differences against a chosen reference and a `range_share` column measuring the share of that slice's floor-to-1 range | A single overall number with the size-band table as the only breakdown; an arbitrary threshold such as n ≥ 20; changing the headline definition to `n > k` only | Measured 2026-08-18: in groups of ≤ 10 the cut-off excludes nothing, so NDCG@10 scores the whole list — random **0.7891**, baseline A 0.8069, baseline B **0.8070**, the two frozen baselines tied to four decimals. Those 91 evaluable groups carry **25.7 % of the metric's weight and 1.6 % of the listings** (693 rows) and hold all 10 degenerate groups. Averaging them in shrinks the measurable lead by a fifth (A − random 0.1000 overall against 0.1256 at n > 10). The cut-off is `k` itself — the condition under which the top-k cut-off excludes anything — so it moves with `k` and is not a free parameter. **The floor is not a constant**: 0.5424 overall, 0.4655 at n > 10, 0.7891 at n ≤ 10, so the roadmap's single-floor framing sentence needs two. The band table stays the fuller answer and shows the effect is an artifact rather than a gradient — the floor is flat at 0.411 / 0.416 / 0.417 above n = 30 |
| 2026-08-18 | **Split-level baseline comparators frozen before any model exists**, pinned by `tests/test_baseline.py` against the shipped feature table: sealed fold **reviews 0.6390 / price+rating 0.6429 / floor 0.5519** (72 groups), sealed `n>10` **0.5951 / 0.5859 / 0.4808** (55), dev pool **0.6432 / 0.6169 / 0.5402** (311), dev `n>10` **0.5903 / 0.5590 / 0.4620** (237). Comparisons are always same-groups; the full-population figures stay in the log as the population record and are never used as a sealed-fold comparator | Computing the comparators after training, alongside the model | Composition moves baseline A by 0.04 across folds, so a sealed-fold model against a full-population baseline compares two populations. Computing the comparator on the sealed fold is **not** tuning — the baselines are unfitted and nothing in the model depends on them — but it is only safe *because* it happens before the model's score exists; afterwards the framing could be chosen to fit. **Recorded surprise: the baseline ordering flips on the sealed fold** — price+rating 0.6429 leads reviews 0.6390 there, where on the population reviews leads by 0.0207. The paired interval covers zero, so it is noise, not a finding; but the model must be reported against **both** baselines rather than against "the one that wins" |
| 2026-08-18 | **The amenity scheme is fixed to `buckets` a priori, before hyperparameter tuning.** Step 4's `buckets` / `flags` / top-50 comparison becomes a **reported ablation run against tuned hyperparameters**, not a selection | The Phase 2 plan: choose the scheme on validation NDCG inside the development pool | Deviates from the 2026-08-17 decision that alternative encodings are chosen on validation NDCG, and the deviation is logged rather than slipped in. It moves in the conservative direction — one fewer thing selected on data — and dissolves the selection cascade that would otherwise have the scheme and the hyperparameters consuming the same four dev folds in sequence. The choice rests on the Phase 2 argument that the bucket map is label-blind by construction. **If `buckets` loses to `flags` on validation, that is reported, not quietly switched** |
| 2026-08-18 | **Groups of ten or fewer are kept in both training and reporting.** They are sliced out for *interpretation* (`report.py` emits `n>k` automatically) but never dropped from either population | Dropping `n<=10` groups before training; dropping them before baseline reporting; dropping the 10 degenerate groups from training | **Training:** those 101 groups hold 693 rows (1.55 %) but only **0.015 % of the label-differing pairs**, and LambdaMART's gradient lives entirely on pairs — dropping them is a no-op to three decimals, so there is nothing to gain and the train/deploy population match is lost. **Reporting:** the slice already yields the number auditably; dropping would raise the headline (A − random 0.1000 → 0.1256) *by deleting the data that does not flatter it*, and would break comparability with the frozen full-population baselines. The 10 degenerate groups contribute exactly zero pairs and are already NaN in `metrics.py`; 48 rows does not justify a code path |
| 2026-08-18 | **Recorded before training: the training gradient and the evaluation metric weight the data almost inversely.** Rows / label-differing pairs / share of the metric, by band: `<10` 1.55 / **0.015** / 25.7 %; 10-30 4.34 / 0.144 / 26.0 %; 30-100 11.63 / 1.202 / 23.4 %; 100-400 30.53 / 11.963 / 17.0 %; 400+ 51.94 / **86.677** / 7.9 %. **Prediction logged in advance: the model should score best on the 400+ band and worst on the small bands.** `lambdarank_norm` true/false becomes a step-2 ablation aimed at this | Row weighting by inverse group size; capping group size by subsampling large groups; ignoring the asymmetry | 86.7 % of the gradient comes from the 31 groups carrying 7.9 % of the metric, and 51.7 % of the metric comes from 0.16 % of the gradient. The model is optimised on close to the inverse of the weighting it is scored on. This is the structural fact the "drop small groups" question was reaching for, and dropping attacks it from the wrong end — it moves 0.015 % of the gradient while deleting a quarter of the metric's weight. Whether `lambdarank_norm` compensates for a ~5,000× pair ratio is measured, not assumed |
| 2026-08-18 | **No group is hand-placed into the sealed fold.** Thessaloniki's sealed-fold NDCG is reported with its interval and no per-city *claim* is made from it; the per-city claim comes from the development out-of-fold | Cherry-picking a >=100-listing Thessaloniki group into the sealed fold to make its per-city number "reliable" | The premise fails on measurement: between-group sd of per-group NDCG is **flat in group size** — 0.147 / 0.164 / 0.161 / 0.164 / 0.171 across the five bands. A group's NDCG is deterministic given its rows, so a 2,088-listing group is **exactly one observation**, no more stable than a 30-listing one. Thessaloniki's own sd is 0.177, so n=4 → ±0.173 and n=5 → ±0.155: the move buys **0.018 of CI width** and costs up to **44.5 % of the city's training data** (its three largest groups are 2088 / 1114 / 665 listings = 44.5 / 23.8 / 14.2 % of its inventory). It would also make the sealed fold chosen by inspection (unreproducible from `assign_folds(frame, seed=0)`), and bias the estimate by size, since NDCG varies 0.807→0.646 across bands. **Out-of-fold predictions are unseen-data predictions** — the dev pool's 24 Thessaloniki groups were each scored by a model that never trained on them (±0.071), and the full 5-fold OOF gives all 30 (±0.063) |
| 2026-08-18 | **Reported as a finding, not engineered around: Thessaloniki's search space is structurally concentrated.** 4,688 listings across only 30 query groups, whose sizes run `2, 2, 6, 8, 8, 8, 8, 10, ... 173, 665, 1114, 2088` — **three groups hold 82 % of the city's inventory** | Treating the small group count as a sampling nuisance to be corrected | Few neighbourhood × room-type × capacity cells clear the minimum size in Thessaloniki and supply piles into a handful of them. The city cannot support a fine-grained per-city ranking claim at any split, and stating that is more informative than manufacturing an interval for it |
| 2026-08-18 | **`train/` split into `lambdamart.py` (pure) and `train.py` (the orchestrator)**, matching `data/` + `data/build.py` and `features/` + `features/build.py`. `lambdamart.py` has no I/O and no `main()`; `train.py` is the sole reader, writer and MLflow logger | One `train.py` doing everything, as the roadmap's step-2 heading implies | The same fit code has to serve a cross-validation fold and a final refit without knowing which it is. Keeping the fit pure is what makes `cross_validate` and `refit` four lines each, and it is the pattern already twice established in the project |
| 2026-08-18 | **`n_estimators` for the refit is the median of the cross-validation `best_iteration`s, taken unscaled.** Measured: 514 / 718 / 158 / 211 → median **362** | Scaling by k/(k-1) for the larger training set; the mean; the maximum | The fold-to-fold spread is **4.5×** (158 to 718) while the fold NDCGs sit in a 0.06 band (0.7026 / 0.7632 / 0.7078 / 0.7119), so the stopping point is barely identified and the metric is nearly flat across it. Scaling a number that uncertain by a folklore factor adds precision the data does not have. The median is the robust summary of four noisy estimates |
| 2026-08-18 | **The five-seed protocol reports a spread of exactly 0.000, and that is an absence of randomness rather than evidence of stability.** At the starting parameters (`subsample_freq=0`, `colsample_bytree=1.0`) LightGBM is fully deterministic — three seeds verified bit-identical. `lambdamart.is_stochastic` detects this and `train.refit` warns; `summarise_seeds` carries a `deterministic` column | Turning on `subsample`/`colsample_bytree` so the seeds would vary; dropping the multi-seed protocol; reporting sd 0.000 without comment | Changing a hyperparameter to make a reporting ritual meaningful is backwards. The variance that exists here is over **data**, and the group bootstrap and the fold-to-fold CV spread already measure it. The roadmap's "run 3-5 seeds and report the spread" assumed a stochastic learner; under these parameters it is vacuous, and printing 0.000 unqualified would read as a very stable model |
| 2026-08-18 | **Permutation test run before reporting the result.** Grades shuffled **within query group** (features untouched, group structure and grade marginals preserved), model retrained, sealed fold scored: **0.5705 / 0.5926 / 0.5407** against a floor of 0.5519, with the real-label control at 0.7541 on the same 500 trees. No structural leakage | Reporting the +0.11 lead on the strength of `check_feature_table` alone | A +0.11 lead over a frozen baseline is large enough that the first question is whether the pipeline leaks. The blocklist check proves no *named* label column is present; the permutation test proves no *unnamed* path from label to features exists either, which is the stronger claim and the one worth having before the number goes into a README |
| 2026-08-18 | **First trained result, sealed fold, recorded.** Model **NDCG@10 0.7530** [0.7148, 0.7903] on 72 groups against reviews **0.6390**, price+rating **0.6429** and a floor of **0.5519** — paired **+0.1139** [0.0795, 0.1534] over reviews, traversing **44.9 %** of the floor-to-1 range against the baseline's 19.4 %. At `n>10` (55 groups): **0.7184** against 0.5951 and a floor of 0.4808, paired **+0.1233** [0.0781, 0.1663]. Development out-of-fold (311 groups) agrees: **0.7209** against 0.6432, paired **+0.0776** [0.0594, 0.0938] | — | The two estimates describe different objects — out-of-fold estimates the *procedure*, the sealed fold the *artifact* — and they agree within their intervals, which is the check that matters more than either number. Per-city from the out-of-fold: Athens 0.7347 (+0.0933), Crete 0.7030 (+0.0598), Thessaloniki 0.7273 (+0.0714 [0.0100, 0.1356]); the model leads in all three and the interval clears zero in all three |
| 2026-08-18 | **The gradient/metric prediction logged on 2026-08-18 is confirmed.** Model `range_share` by band, out-of-fold: `<10` 0.267, 10-30 0.255, 30-100 0.394, 100-400 0.524, **400+ 0.589** — monotone in group size above 30, exactly as predicted from 86.7 % of the gradient sitting in the 400+ band. At `n<=10` the paired lead over the baseline is +0.0337 with an interval **[-0.0020, 0.0688] that touches zero** | — | Predicted **in advance** from the pair-count table, which is the part that counts. Note the raw NDCG is *not* monotone (the floors differ by 0.38 across bands); only `range_share` is, which is why the floor is reported per slice. The model is close to unable to beat the baseline in the quarter of the metric where the cut-off does not cut — consistent with those groups holding 0.015 % of the training gradient |
| 2026-08-18 | **Establishment features top the importance chart, as Phase 1 predicted: 8 of 61 features carry 29.5 % of total gain** (`number_of_reviews` alone 8.8 %). Spatial 10.0 %, neighbourhood 8.7 %, the 20 amenity columns 14.1 % | — | Notebook 02 §4 measured the label as substantially establishment-driven, so a model that re-derives "established listings sell" is confirming the label's structure, **not** discovering a ranking insight. The frozen baseline is the defence and it holds: baseline A is that signal alone at 0.6390, the model reaches 0.7530, so the other 71 % of gain is worth +0.114. The ablations in step 4 test whether the spatial and neighbourhood blocks earn their 18.7 % |
| 2026-08-18 | **The 0.7530 result is from untuned defaults, and is labelled as such.** `num_leaves=31`, `learning_rate=0.05`, `min_child_samples=20`, `n_estimators=2000` capped by early stopping — `lambdamart.DEFAULT_PARAMS` unchanged. The **only** quantity chosen from data was the stopping iteration (CV median 362). No sweep was run; the roadmap's "record the search, including the configurations that lost" is still an open box | Running a sweep before reporting a first number; presenting the default-parameter result without saying it is untuned | A first number from stated defaults is a legitimate and honest result, and it is the right thing to have before a sweep exists — it is the thing a sweep has to beat. What is not legitimate is letting it read as tuned. Expect little movement: fold `best_iteration` ranged 158-718 while fold NDCG stayed inside a 0.06 band, which is a plateau, and that is itself the reportable finding if it holds |
| 2026-08-18 | **Measured before claiming the result is meaningful: the model is *not* an establishment re-derivation.** Development out-of-fold, 311 groups — `number_of_reviews` alone **0.6432**; a GBM on the **8 establishment features only 0.6660** (+0.0228 [0.0101, 0.0361]); a GBM on the **53 non-establishment features only 0.7031** (+0.0599 [0.0408, 0.0772]); full 61 features **0.7209** (+0.0776). At `n>10`: 0.5903 / 0.6119 / 0.6561 / 0.6816 | Reading the importance chart as the answer; assuming the 29.5 % establishment gain share meant establishment was doing the work | **Gain importance is not marginal contribution when features are correlated.** The establishment block carries 29.5 % of gain yet a model denied it *entirely* recovers 77 % of the full lift over baseline, while a model given only establishment recovers 29 %. Establishment is close to saturated by review count alone — the other seven establishment features add 0.023 between them. The two blocks are mildly subadditive (0.023 + 0.060 against a joint 0.078), so they overlap somewhat but the non-establishment signal dominates. This is the check that separates a real result from an elaborate restatement of Phase 1, and it should be run before the README is written, not after |
| 2026-08-18 | **Hyperparameter search is random over 35 configurations, selected on the paired out-of-fold per-group vector, not on the 4-fold mean.** `train/sweep.py`. Configuration 0 is the defaults, evaluated by the same code on the same folds as its challengers | Grid search; Bayesian optimisation / TPE via Optuna; successive halving | The 4-fold mean carries **sd 0.0281, SE 0.0141, 95 % ±0.0276** while real differences on this plateau are 0.005-0.015 — selecting on it selects noise, however good the optimiser. The fix is the objective, not the search: 311 out-of-fold groups compared **paired** against the defaults, which removes most of the variance because similar configurations agree group by group. **Against Bayesian specifically:** TPE's advantage is sample efficiency when an evaluation is expensive (ours costs ~3 minutes) and its assumption is that an observed score is informative about the configuration (at this noise level it is not) — it would converge confidently onto a lucky fold draw. It also adds a dependency to a project that rejected `squarify`/`geopy`/`requests`, and produces a worse artifact than a table with the losers in it |
| 2026-08-18 | **No early-termination policy (bandit / median-stopping / truncation-selection), local or on Azure.** What is used instead is `CATASTROPHE_MARGIN` — abandon a configuration after fold 1 only if it is **3 sd (0.084) below the best fold 1 seen**. Pruned configurations stay in the results table marked `pruned` | Azure ML sweep with a bandit policy; median stopping applied across CV folds; no guard at all | Those policies compare a trial's *intermediate* metric against its peers, and **a trial here has no trajectory** — four fits pooled into one number. Giving them something to act on means reporting per-fold, which breaks the paired-OOF selection that is the only reason the search can resolve anything. Pruning on a single fold prunes on sd 0.0281, roughly a coin flip between configurations within 0.02 of each other, which is most of the plateau. And 35 trials take ~90 minutes, so there is nothing to save. The termination that does belong is LightGBM's own early stopping, already in every fit. The guard is deliberately 3 sd — it skips broken configurations and cannot plausibly discard a plateau winner |
| 2026-08-18 | **Azure ML sweep is scoped to step 5 as a workflow demonstration only**, on a small budget, with the hyperparameters explicitly not sourced from it | Running the real search on Azure with a bandit policy and local compute | Sweep orchestration is service-side and `compute: local` is a command-job path, not a sweep target — to be verified before the step-5 YAML is written rather than assumed. Independent of that, running the real search in the cloud contradicts the project's own rule (CLAUDE.md: training runs locally, Azure demonstrates the workflow) and puts a meter on it. A sweep job with a bandit policy is a good *demonstration*, treated exactly as the sentiment demo is |
| 2026-08-18 | **Acceptance rule declared before the search ran: keep the defaults unless the winner's paired interval against them excludes zero.** The winner's out-of-fold gain is the maximum of ~35 draws and is optimistically biased; the **sealed fold is read exactly twice in this project — defaults (done) and the tuned winner** — and that count is declared now, before any winner is known. The defaults result stays the headline; the tuned result is reported beside it as "what tuning bought" | Adopting the best point estimate; letting the tuned model become the headline; leaving the number of sealed reads open | The maximum of noise is positive by construction, so a point-estimate win is not evidence. Declaring the sealed-read count in advance is what keeps the holdout meaningful — an undeclared drift to "just one more look" is how a holdout stops being one. `sweep.accept` implements the rule and `tests/test_sweep.py` pins that a larger but wilder gain loses to a refusal |
| 2026-08-18 | **Sweep result: config 34 accepted, +0.0142 [0.0027, 0.0258] paired over the defaults.** 6 of 34 challengers cleared zero against ~1 expected under the null; total spread across all configurations 0.0391. **`lambdarank_truncation_level` is the only parameter that matters** — spearman **+0.613** with out-of-fold NDCG, means `0.7023 / 0.7137 / 0.7217 / 0.7256 / 0.7262` at levels 10/20/30/50/100, monotone and plateauing by 50. `min_child_samples` correlates +0.008 | — | The mechanism is the pair-count table: truncation level caps how far down a ranked list a pair still generates gradient, so at level 10 nearly every pair in the groups carrying 86.7 % of the gradient is discarded. LightGBM's default of 30 already sits near the plateau, which is why the search found so little. **Tuning is a rounding error next to the modelling** — +0.0142 against the +0.0776 the features bought. The defaults stay the headline; the winner is reported beside it. **The second sealed read is deliberately unspent**: it should measure the configuration that ships, so it waits for step 4's ablations to settle the feature set |
| 2026-08-18 | **Notebook 04 reads `data/train/sweep_results.csv` rather than re-running the search**, written by `train.py --sweep N`. `train.py` stays the single orchestrator for `train/`; `sweep.py` stays pure | A `main()` in `sweep.py`; the notebook re-running a smaller search; committing the results table | 35 cross-validations is far too slow for a notebook that must run clean top-to-bottom, and a *smaller* search in the notebook would print different numbers from the ones the decision was made on. The results table takes the same contract as the feature table — gitignored, rebuilt by one documented command — and the convention that `train/` has exactly one `main()` is preserved by making the search a flag on it |
| 2026-08-18 | **Step 4 ablations, development out-of-fold, 311 groups, paired against the full model. Exactly one block is irreplaceable.** `minus establishment` **−0.0178** [−0.0308, −0.0048] is the only ablation whose interval clears zero. Everything else covers it: `minus neighbourhood` **+0.0040** [−0.0052, +0.0135], `minus spatial` **−0.0050** [−0.0141, +0.0051], `minus spatial + neighbourhood` **+0.0008** [−0.0094, +0.0119], `minus amenities` (20 columns) **−0.0012** [−0.0120, +0.0096] | — | **Notebook 03 §2 predicted this in advance** from within-group/overall variance ratios of 0.015–0.12 for the spatial and neighbourhood blocks, and the prediction holds: six features can be removed for a point estimate of **+0.0008**, i.e. nothing. That is a real finding and a defensible cut. The amenity result was *not* predicted and is the more striking one — 20 columns removable at −0.0012 |
| 2026-08-18 | **Amenity scheme bake-off: `buckets` is kept, as chosen a priori, and the honest reading is that the whole block reduces to one number.** Out-of-fold: `buckets` (the shipped choice) 0.7209, `flags@50` 0.7260 (**+0.0052** [−0.0041, +0.0145]), `count` alone 0.7180 (**−0.0029** [−0.0134, +0.0076]). Neither challenger's interval clears zero | Switching to `flags@50` on its higher point estimate; dropping the amenity block entirely | The scheme was fixed to `buckets` before tuning (2026-08-18) on the label-blind-construction argument, so this comparison is a **reported ablation, not a selection** — and `flags` does not clear zero, so nothing overturns the a-priori choice. The substantive finding is that **`amenity_count` alone is statistically indistinguishable from 19 buckets or 50 flags**: the entire 20-column block carries no measurable signal beyond "how many amenities does it list". `fit_vocabulary` was verified against the shipped column first — the rebuilt `amenity_count` matched row-for-row across all 44,684 rows, so the swap is clean |
| 2026-08-18 | **The shipping feature set stays at all 61 features; no block is cut.** The ablation is reported as a *finding* rather than acted on | Cutting spatial + neighbourhood (6 features, predicted in advance and confirmed); cutting amenities down to `count` (19 more) | **No cut showed a detectable gain, so choosing one on a point estimate that covers zero is selection on noise** — the same winner's-curse the sweep's acceptance rule exists to refuse, and it would be inconsistent to apply the rule there and not here. Keeping 61 also means the second sealed read changes exactly **one** variable against the first (the hyperparameters), which is what makes that read interpretable. "These blocks are removable at no measured cost" is a stronger and more honest statement than removing them |
| 2026-08-18 | **Cold-start measured, and the finding is that the headline metric cannot see the problem.** Development out-of-fold, 311 groups, 5,884 never-reviewed listings (16.5 %). **(a) The target's bias**: grade-0 rate 8.12 % against a 3.61 % base — **2.25×** over-represented (the roadmap's 2.38× was on the full population). The larger effect is the whole lower half: cold-start listings are 48.4 % grade 0-1 against 24.7 % for reviewed. **(b) The ranker amplifies it**: cold-start listings sit at within-group percentile **0.195** where their own grades place them at **0.384** — the model ranks them **19 percentile points below what the target itself says they deserve**. Baseline A is worse at −0.271. **(c) Deserving cold-start listings graded 3+ reach a top 10: ideal 17.2 %, random 9.6 %, model 5.8 %, baseline A 3.0 % — both rankers are worse than random.** **(d) Group NDCG@10 is blind to all of it**: in the most cold-start-heavy band (30 %+) the model scores 0.7649 and beats the baseline by +0.0882 [0.0419, 0.1374] | Reporting cold-start as an NDCG slice; deferring the measurement to Phase 5 | NDCG@10 with exponential gain is dominated by the top slots, and the model fills them with established listings that are genuinely high-grade — so the metric looks *better* in cold-start-heavy groups while the cold-start listings themselves are buried. **A mitigation built against an unquantified problem cannot be shown to have helped**, which is why this is measured now rather than in Phase 5. The honest headline is (c): the model is **below a random ranker** at surfacing never-reviewed listings that the target itself grades 3+, and the +0.0882 group-level win in those same groups must never be quoted as evidence the model handles cold start |
| 2026-08-18 | **Correction to the 2026-08-18 ablation reading: only 2 of the 6 spatial/neighbourhood features are structural conditioners.** Measured within-group/overall variance: `nbhd_listings` **0.001** and `nbhd_median_price` **0.003** are constant inside a group by construction (neighbourhood is in the query-group key), but `km_to_nearest_anchor` 0.092, `density_1km` 0.126, **`km_to_neighbourhood_centroid` 0.444** and **`price_vs_nbhd` 0.631** all vary substantially within groups | The earlier blanket claim that the spatial and neighbourhood blocks are conditioners and no amount of data would rescue them | The blanket claim was generalised from notebook 03's block-level summary instead of the per-column numbers, and it is wrong for the two features that vary most. For `km_to_neighbourhood_centroid` and `price_vs_nbhd` the ablation null is a **signal-or-power** result, not a structural one — more query groups genuinely could resolve them. Also confirmed while checking: `km_to_neighbourhood_centroid` is **already** the leave-one-out centroid of the listing's *own* neighbourhood (`spatial.py:149`), not a city-wide one |
| 2026-08-18 | **Cold-start diagnosis: the deficit is a cohort-level offset, not a collapse in discrimination.** 13 of 61 features carry no information for a never-reviewed listing and hold **37.6 %** of total gain (all six `review_scores_*`, plus the establishment block). But within-group Spearman of score against grade is **+0.185 among cold-start listings** versus +0.275 among reviewed — about two-thirds of the discrimination survives | Concluding from the worse-than-random top-10 result that the model had learned only review signal | The model can still order cold-start listings against each other; what it does is apply a ~19-percentile penalty to the whole cohort, and that penalty is **correct on average** (48.4 % of them are graded 0-1). LambdaMART is doing the pairwise-optimal thing. **This changes what a mitigation looks like**: not better cold-start features first, but a product decision about whether the ranking should apply the cohort mean at all — an exploration or new-listing boost, which is what marketplaces actually ship. Phase 5 has a target and an instrument: lift top-10 reach for grade-3+ cold-start listings from 5.8 % past the 9.6 % random reference |
| 2026-08-18 | **SECOND AND FINAL SEALED READ, 2026-08-18: the tuning gain did not replicate.** Config 34, all 61 features, refit on folds 1-4, five seeds. Sealed fold **0.7514 ± 0.0036** overall against the defaults' **0.7530**, and **0.7236 ± 0.0057** at `n>10` against the defaults' **0.7184**. Development out-of-fold had promised **+0.0142**; the holdout delivers **−0.0016** overall and **+0.0052** at `n>10`, both inside the seed spread. Paired against the baselines the model still wins decisively: **+0.1123** over review-count and **+0.1085** over price+rating overall | — | **This is the winner's curse, observed rather than argued.** The +0.0142 was the maximum of 34 challengers and the maximum of noise is positive; out of sample it evaporated. The pre-declared decisions — defaults stay the headline, the tuned model is reported beside it, the holdout is the only unbiased measure of a selected gain — are all vindicated by their own test. **Both declared reads are now spent and the holdout is closed.** The headline for Phase 3 remains the untuned **0.7530**; the tuned model is a footnote showing tuning bought nothing measurable |
| 2026-08-18 | **The five-seed spread is finally meaningful, and the model is stable — except where it was predicted not to be.** Config 34 is stochastic (`subsample=0.79`, `colsample_bytree=0.55`), so seeds differ for the first time: sealed sd **0.0036** overall and 0.0057 at `n>10`. **Thessaloniki's sd is 0.0395 — eleven times the overall figure — across its 4 sealed groups**, with three of five seeds scoring *below* the random floor | — | The earlier refusal to make a per-city claim from 4 groups was argued from between-group variance (±0.183 predicted). This is the same conclusion arriving empirically from a completely different direction: the *seed* alone moves Thessaloniki's sealed number by 0.078 (0.6438 to 0.7219). A number that a random seed can move that far is not a measurement. Athens (38 groups) sd 0.0034 and Crete (30 groups) sd 0.0107 behave normally |
| 2026-08-18 | **`mlflow.db` and `mlruns/` stay out of git; run evidence is published as CSV artifacts and one figure instead.** `mlflow.db` added to `.gitignore` beside `mlruns/` | Committing `mlflow.db` so a reader can inspect the training runs | It is a **binary SQLite file that grows with every run**, so it bloats git history irreversibly and diffs to nothing. Worse, it is **not portable**: its artifact URIs are absolute paths into this machine's `mlruns/`, so a cloner gets a database whose every artifact reference is broken. The inspectable form already exists and is text — the ten per-run CSVs (comparison tables, fold report, CV report, feature importance, learning curves) plus notebook 04, which executes clean and renders all of it. For the portfolio, a screenshot of the MLflow UI is the right artifact, not the store behind it |
| 2026-08-18 | **Per-iteration learning curves recorded and logged as an MLflow artifact** (`learning_curves.png` + `.csv`), and rendered in notebook 04 §3. `cross_validate` now returns a fourth element carrying LightGBM's validation NDCG@10 at every boosting iteration for each development fold | Leaving the "the stopping point is barely identified" claim as prose backed only by the four `best_iteration` values | The claim was an assertion; the figure is evidence. All four folds are within 0.02 of their final value by iteration 150, while their chosen stopping points scatter across **158 / 211 / 514 / 718** — the markers land wherever a hundred-round patience happened to fire on a flat surface. **The figure also predicts the §9 result before it happens**: a 35-configuration search over a surface this flat was never going to find much, and out of sample it found nothing |
| 2026-08-18 | **"Pin the cloud environment from uv.lock" is not a text operation, and treating it as one failed the first Azure job.** `requires-python = ">=3.11"` means uv.lock carries a resolution per interpreter — both `numpy 2.4.6 (marker python_full_version < '3.12')` and `numpy 2.5.1 (>= '3.12')`. A regex taking the first version per package pinned `numpy==2.5.1` against `python=3.11`; pip refused it and the image build died. Two packages were wrong the same way: numpy (→ **2.4.6**) and scipy (→ **1.17.1**). Fixed by pinning from the **resolved environment** (`importlib.metadata` inside the venv), with the regenerate command recorded in `environment.yml` | Scraping uv.lock; using a curated Azure ML image to sidestep the build | The lock is a resolution *space*, not a resolution — the markers are the content, and dropping them produces a manifest that is internally inconsistent rather than merely stale. Local is Python 3.11.15, so `python=3.11` in the conda file is load-bearing. **Also learned: a failed build still creates the ACR.** The registry existed after the failure with no job ever having run, so the $5/month meter starts at the first *attempt*, not the first success |
| 2026-08-18 | **Cross-platform determinism held exactly, and the caveat declared in advance was unnecessary.** Before the first cloud run it was recorded that LightGBM's multithreaded histogram construction is not order-guaranteed across a 10-thread macOS ARM build and a 4-vCPU Linux x86 one, so any divergence from the local 0.7530 should be read as a platform artifact. **There was no divergence**: the cloud reproduced `0.7026 / 0.7632 / 0.7078 / 0.7119` at `514 / 718 / 158 / 211`, median 362, and its `dataset_digest` matched local (`8c74b2840854`) | Not declaring the caveat and reporting whatever came back | Declaring it beforehand is what made the comparison a check rather than a rationalisation — had the numbers differed, the explanation would already have been on the record instead of being invented afterwards. The caveat is kept in `azure_setup.md` for that reason, marked as answered, but it must not be repeated as a live risk |
| 2026-08-18 | **Azure ML does not serve MLflow 3's LoggedModel API; model logging falls back to a raw booster artifact.** `mlflow.lightgbm.log_model` posts to `/api/2.0/mlflow/logged-models`, which returns **404** against the workspace and failed a run whose training had completed and whose tags had already landed. `train._log_model` now attempts the flavoured model, and on any server refusal writes `booster.txt` as an artifact plus a `model_logging` tag saying so | Dropping model logging in the cloud entirely; downgrading to MLflow 2.x | The model is preserved either way; only the flavour metadata is lost, and the run reports the degradation instead of dying at the last line. Attempting the flavoured path first keeps local behaviour intact, where it works. **Consequence for step 8:** deploying an MLflow-flavoured model to a managed endpoint was the clean route and may be unavailable — expect the scoring script to load the booster directly |
| 2026-08-18 | **Azure AI Language billing has a one-record minimum per document, which makes the free tier 4x smaller than a length-based estimate suggests.** A text record is 1,000 characters **rounded up, minimum one**. This corpus's median review is 195 characters, so nearly every review costs a whole record: 5,000 records/month is worth about **5,000 short reviews, not 19,000**. Measured against the real selection, 393 reviews price at exactly 393 records | The estimate first written into the module docstrings, which divided length by 1,000 and concluded the roadmap's "<= 500 documents" was conservative by ~40x | It is conservative by ~10x, not 40x. The error was caught by running `--estimate-only` before spending anything, which is the reason that flag exists. `text_records` had the rule right (`max(1, ceil(...))`); the prose around it did not, and the prose is what a budget decision gets made from. Corrected in both docstrings and pinned by `tests/test_sentiment.py`, where `[195] -> 1` is a parametrised case |
| 2026-08-18 | **The sentiment demonstration scores one whole query group (24: 23 listings, 393 reviews), not a sample across many.** Cache is checked before the network on every run; `--refresh` is the only way to re-bill; `--max-records` refuses **before** calling, defaulting to a fifth of the monthly allowance. The billable call lives in a new `cloud/` package, kept out of `data/`, `features/`, `train/` and `evaluate/` | Sampling the top-N most-reviewed listings across several groups; putting the caller in `features/sentiment.py` alongside the pure transforms | Scoring top-N within a group makes "has sentiment" a proxy for review count — the label's dominant driver — so the comparison would measure missingness rather than sentiment, which is the banned "has price" flag pattern. A whole group preserves the only comparison a pairwise ranker makes. The `cloud/` split exists so the boundary is visible: nothing in the training pipeline imports from it, and a reader can see at a glance which module can spend money |
| 2026-08-18 | **Step 6 descoped: the sentiment demonstration is designed, tested and priced, but was never run.** No call was ever made to Azure AI Language and no number in this project comes from it. `features/sentiment.py`, `cloud/sentiment.py` and 19 tests remain as the artifact | Moving the resource to West Europe and running it; running it against a filtered English-only subset | Three problems surfaced in sequence — a budget wrong by 5x (records bill with a one-document minimum), **72 % of documents not English against a hardcoded `language="en"`**, and a region that offers the account *kind* but not the sentiment *capability*. All three were fixable and the third was a ten-minute region move. The judgement was about value, not difficulty: **the scientific question closed in Phase 2 on measurement** (+0.015 within groups, −0.026 at the rating ceiling, beaten 2.6× by `review_scores_value`), so the run would have added only a *fourth* Azure workflow demonstration behind command jobs, versioned data assets and a registered environment — the least valuable item left in the phase. **The README must state plainly that it was not run**; a demonstration designed but not executed is a respectable artifact, one implied to have run is a lie |
| 2026-08-18 | **Recorded because it is not obvious: in Azure, a region offering a resource *kind* does not mean it offers every *capability* of that kind.** `Microsoft.CognitiveServices` accounts create successfully in `italynorth` — ours did, with no warning from the CLI — but sentiment analysis is not available there, which surfaces only in Language Studio or at call time | Assuming CLI creation success implies the feature works | The `az cognitiveservices account create` call returned a healthy resource with an endpoint. Nothing in the CLI path indicates the capability gap. **Check feature-level region availability before creating the resource, not after** — and prefer co-locating cognitive services in a large region (West Europe) rather than beside the workspace, since `docs/azure_setup.md` already notes neither service needs the other's region |
| 2026-08-18 | **MLTable would not fix the categorical-dtype problem at inference, and was rejected for that purpose.** The five `category` columns break MLflow signature inference and would arrive at a served endpoint as plain objects | Registering the feature table as an MLTable asset to carry column types | MLTable is a **data-loading** specification: it governs how a *job* reads an asset. The categorical failure happens at **inference**, where MLflow deserializes a JSON request into a DataFrame — the endpoint never reads the data asset, so MLTable is not in that path at all. Parquet already preserves the pandas `category` dtype, which is why training works unaided. The fix belongs to the **model's input contract**: a pyfunc wrapper that restores the category dtypes using the training categories, in training order. MLTable remains a reasonable upgrade on its own merits (an explicit typed schema for the asset), just not for this |
| 2026-08-18 | **The endpoint returns a ranked list, not raw scores.** `cloud/score.py` takes `{"listings": [...]}` — the candidate set for **one** query — and returns `{"ranked": [{"id", "score", "rank"}], ...}` already ordered. Custom scoring script, not a no-code MLflow deployment | No-code MLflow deployment of the `mlflow_model` asset, which was proven to register successfully | **A ranker's score has no absolute meaning**; it is only comparable within one query. Returning unordered scores invites exactly the cross-query comparison the model cannot support, so the ordering is applied server-side and the contract makes the query boundary explicit. No-code deployment also returns predictions in input order with no id, offers nowhere to validate a request, and cannot express the five `category` columns in an MLflow signature |
| 2026-08-18 | **Correction, measured: the categorical serving risk is an unseen level, not code order.** An earlier entry claimed pandas category *codes* shift silently at inference. **Two of three assumptions were wrong.** (a) Order does **not** matter — the booster carries training levels in `pandas_categorical` and re-maps by label; a request covering 2 of 3 levels scored identically to 0.000000. (b) A plain string column fails **loudly** (`train and valid dataset categorical_feature do not match`). (c) **An unseen level is the silent one**: `room_type="Houseboat"` predicts with no error, **0.1083** from truth, because `set_categories` makes it NaN and the model scores it as *missing* | Shipping the original explanation, which would have been wrong in the docs and right in the code by accident | `restore_dtypes` is still necessary — for (b) to work at all and (c) to be rejected — but the justification in the docstring was invented rather than measured, and the measurement contradicted it. Recorded because the code would have looked correct either way |
| 2026-08-18 | **Step 8 closed with the endpoint deleted in the same session.** Timeline: created 18:46:34Z (free — an endpoint with no deployment does not bill), instance provisioned 19:02:46Z, invoked 19:03:22Z, delete issued 19:03:51Z, confirmed gone 19:11:44Z. ~9 minutes on `Standard_DS2_v2` at $0.1360/hr ≈ **$0.02**. Request/response evidence in `docs/endpoint_demo/` | Leaving it up to screenshot later | The shape of the timeline is the lesson: **16 minutes to provision, 29 seconds of use, 8 minutes to delete.** The demonstration is trivially cheap; the failure mode gotcha #6 names costs ~$98/month for one idle DS2_v2. Note also that **teardown took 8 minutes** — "delete it afterwards" is not instant, so it has to be started deliberately rather than assumed done |
| 2026-08-19 | **The endpoint demonstration is rebuilt around meaning, not existence.** The first pass proved an endpoint answered: ids, scores, ranks. `cloud/demo.py` now builds requests from **sealed-fold** listings and joins the response back to the held-out grades, so an ordering can be read against the truth, and reports the endpoint against both frozen baselines and a random floor **on the same candidate set** | Screenshotting a bare `{"id","score","rank"}` array and calling it a demonstration; sending the grade to the endpoint to make the join easy | A response with no truth beside it is indistinguishable from a shuffle. The scoring contract is right to withhold the target — a service has no business returning it — so the join belongs on the client. `build_payload` **raises** if a feature list names `grade`, `blocked_fraction_90`, `query_group` or `cluster_id`: that request would return a beautiful ranking and nothing in the output would say the demo had gone circular |
| 2026-08-19 | **Demo queries are chosen by a label-blind rule: largest sealed group ≤30 listings per city, ties to the lower group id.** Athens 79 (29), Crete 305 (25), Thessaloniki 24 (23) | Choosing the query after seeing its per-query NDCG | Same reasoning as reporting the best seed. The rule returns Athens at **0.5976** — the weakest of the three — and it is kept and labelled as the weakest. The trio brackets the sealed estimate (0.5976 / 0.7962 / 0.8095 against 0.7530) rather than flattering it |
| 2026-08-19 | **A single query's NDCG is an anecdote and is labelled as one in the output itself.** Every rendered block ends by naming the sealed-fold estimate, 0.7530 [0.7148, 0.7903] over 72 groups | Quoting a per-query number as a result | One group carries no interval worth having. The per-query number exists so a reader can check the ordering against the rows in front of them, which is a different job from estimating quality |
| 2026-08-19 | **The counterfactual is the part of the demonstration a fixed ranking cannot fake.** The top listing is re-sent with its review history stripped and everything else held. Thessaloniki: rank **1 → 15** of 23. Crete: **1 → 1**, no movement | Showing only the query where the rank moves | The contrast is the finding. It agrees with notebook 04's ablations — review/establishment signal is what the model leans on hardest, but not uniformly, and a demonstration that showed only the dramatic case would misrepresent that |
| 2026-08-19 | **Scoring-path bug found by the cold-start demo request: a feature column that is present and `null` for every listing crashed the container.** `json.loads` gives it `object` dtype and LightGBM raises `pandas dtypes must be int, float or bool` — unhandled, so a 500 rather than a message. Fixed in `restore_dtypes`, which now coerces object-dtype numeric columns and raises a named error for a genuinely non-numeric value | Leaving the difference between an absent column and an all-null column to the serialiser | The two requests are semantically identical and the earlier "sparse" demo passed only because its missing columns were **absent** — `reindex` gives those `float64` NaN. Any client that serialises a row with nulls, which is the normal thing to do, hit a 500. Three regression tests pin it, including that both forms produce the same matrix |
| 2026-08-19 | **The console searches on `city × neighbourhood × room type × guests`, which is the query-group key rather than a skin over it.** `features/groups.py` builds the group from exactly those four because they are "what a guest would have typed", so choosing them selects the candidate set. 112 searches resolve across the 74 sealed groups | Inventing a search form that filters listings independently of the grouping the model was trained under | A form that filtered rows would hand the ranker a candidate set no query group ever contained, and the NDCG beside it would be measuring a population the grades were not cut against. Making the form the key means the demo cannot drift from the model. `guests_to_tier` is pinned to `groups.capacity_tier` by a test — two implementations of one rule is one too many |
| 2026-08-19 | **41 % of searchable combinations resolve into a pooled group, and the console says so instead of hiding it.** The banner names the count of neighbourhoods the group actually spans and highlights the listings that matched the literal search | Presenting a pooled group as though the chosen neighbourhood narrowed the competition | It is the fallback cascade working as designed (rung usage 44,395 / 255 / 34), not a defect — but a guest who picks a thin neighbourhood is competing city-wide, and a demo that implied otherwise would misrepresent what the group is. The width columns are **observed** (`nunique` over the group's members), never a claim about which rung fired |
| 2026-08-19 | **Listing titles are shown beside the hashed id.** `name` is a kept column in the data contract — `host_name` is the one that is stripped — and it is joined back from the processed layer for display only; it is not in the served feature list, so `build_payload` cannot send it | Leaving the demonstration as columns of hashed ids | A ranking of twelve-character hashes is unreadable, and the point of this pass was legibility. The id stays hashed; the public title is not PII |
| 2026-08-19 | **Reset is always live, and reloads the group at the listing you are on.** Three bugs, all found by driving a real browser rather than reasoning about the code: it snapped back to the top-ranked listing, it left a refused edit's error on screen, and it was disabled until a re-rank *succeeded* — so after a rejected edit, the moment you most want it, the button did nothing | Reasoning about DOM behaviour instead of exercising it; gating a restore action on prior success | "Put this back" is always a valid request, and the state most needing a reset is the broken one. The bug was invisible in isolation — reset worked fine in a clean sequence — and only appeared when the probe drove search, listing switch, preset, bad edit and reset in the order a person would |
| 2026-08-19 | **The console searches the whole ranked population, not just the sealed fold, and stamps every result `HELD OUT` or `TRAINED ON`.** 516 searches across all 393 groups and all 75 neighbourhoods, against 112 / 74 / 58 before | Restricting the search to fold 0 so that every number shown is out-of-sample | The restriction looked like the conservative choice and was quietly misleading: **17 of 75 neighbourhoods have no sealed listing at all — 15,569 rows, 34.8 % of the population — including the largest neighbourhood in every city.** Central Thessaloniki (4,162 listings, 89 % of the city) was simply absent from the picker. That is the grouped split working as designed — whole connected components move together and a big neighbourhood is a big component — but a search form missing half the map misrepresents the data more than a labelled in-sample result does |
| 2026-08-19 | **For a trained-on group no metric is computed at all — it is absent from the response, not hidden in the page.** Rank movement survives; NDCG does not | Rendering the metric greyed out, or behind a warning | A warning can be cropped out of a screenshot and a greyed number is still a number in the DOM. If the value never enters the payload there is nothing to leak. Movement is kept because "it fell 1 → 15 when I stripped its reviews" is a claim about behaviour, not an estimate of quality, and it is the one thing an in-sample group still demonstrates honestly. The CLI and `--capture` remain sealed-only: `group_listings` defaults to `sealed_only=True` and anything that reports a number uses that default |
| 2026-08-19 | **Recorded, not fixed: the sealed fold is not neighbourhood-representative.** Fold 0 holds 18.1 % of Athens, 24.4 % of Crete and **1.9 % of Thessaloniki** (88 rows of 4,688), and 17 neighbourhoods have zero representation | Re-splitting to balance neighbourhoods | Re-splitting would invalidate every number in Phase 3, and the split's own strata (city × group-size band, on **group counts**) were chosen and defended in step 1 — group counts came out balanced at 74/79/77/84/79. The row-level imbalance is the price of moving whole components, which is the thing that makes the split valid at all. It belongs in the limitations: the 0.7530 estimate is out-of-sample and grouped correctly, but it is largely an Athens-and-Crete number, and the four Thessaloniki groups in it were already flagged as too thin to carry a per-city claim |
| 2026-08-19 | **Reverted: the console picker is sealed-fold only again, and the gap is printed instead of filled.** 112 searches over the 74 held-out groups; `coverage()` reports 17 of 75 neighbourhoods and 34.8 % of listings unsearchable, with the largest hidden neighbourhood per city named on the page | Offering all 393 groups with a `TRAINED ON` label and metric suppression, which is what the previous entry decided | The labelled version worked and was still the wrong shape for a *demonstration*: it spent a warning banner, a tag, a suppression branch and a whole test group on making unusable results safe to look at, when the honest move is not to offer them. A sentence stating what is missing carries the same information at a fraction of the machinery. The suppression code, the `sealed` plumbing and `is_sealed` were deleted rather than left dormant |
| 2026-08-19 | **A second container image for the console, separate from the scoring image.** `docker/Dockerfile`, 857 MB, no Azure dependency and no credential; ships the booster, the sealed fold **already resolved**, and a precomputed coverage report | One image serving both the endpoint and the console | They answer to different callers. The scoring image is built by Azure from `pipelines/score_environment.yml` and needs `azureml-inference-server-http`; the console needs pyarrow and a demonstration dataset and must never hold a key. Shipping the resolved fold rather than the feature table also removes a 25-second connected-components run from every container start, for an answer that cannot change. Verified reproducing group 24 at 0.809461900603779 — digit for digit against local |
| 2026-08-19 | **Bug found by the container, not by the tests: `group_listings` reached past the bundle for the feature table.** The bundle path was wired into `sealed_table` and `coverage` while the one function every scored request goes through still read `ranked_table()` | Trusting that routing two of three entry points was enough | The tests all run in the repo, where the feature table exists, so nothing failed. Only an environment that genuinely lacks the file could catch it — which is the argument for the container being a test surface rather than only a deliverable. Routing it through `sealed_table` also made the refusal of a trained-on group the single guard it should always have been |
| 2026-08-19 | **`endpoint_address` refuses a URI that does not end in `/score`, and prints the corrected one.** Hit live: the Studio endpoint page lists the **Swagger URI** one line below the REST endpoint, and a POST to `/swagger.json` returns `HTTP 424: {"message": "The method is not allowed for the requested URL."}` | Letting the call fail and reading the response | 424 from the front door wrapping a 405 from the inference server, and **nothing in the chain names the URL or the mistake** — the endpoint is healthy, the key is right, the payload is right, and the error points at none of it. Guessing costs a metered session. The guard is a two-line check against a five-minute misdiagnosis, and it is the only client-side error in the project that can be produced without any code being wrong |
| 2026-08-19 | **The endpoint's cloud/local parity is *recomputed* in notebook 04 §10, not asserted.** The section loads the responses the live endpoint actually returned, re-scores the same requests through the local scoring script, and prints `max \|score difference\|` — **0.0** on all three queries, identical ordering | Writing "the cloud reproduced local exactly" in prose | The endpoint is deleted the same session, so a notebook that called it would not run again — but the committed responses do, and checking them against a fresh local run is a stronger claim than a sentence and survives a kernel restart. Third independent confirmation after the training job and the first invocation; the pre-declared platform-drift caveat was never needed, which is worth stating rather than quietly dropping |
| 2026-08-19 | **A losing query is kept in the write-up and explained by measurement.** Athens / Kypseli / 10 guests: **0.6040** against a **0.6563** random floor, beaten by both baselines, grade-1 listing at rank 1 | Illustrating the endpoint with the queries where it does well | The cause is arithmetic, not rhetoric: group 161 is graded `{1:×1, 2:×7, 3:×6, 4:×1}`, so 13 of 15 listings are a 2 or a 3, two-thirds of the group reaches the top ten whatever the ordering, and the whole spread between best and worst ranker is 0.08. Kalamaria, same model, same session, reaches 0.8785 on 43 listings with five distinct grades. **That variance is the argument for the interval** — and a screenshot of it is more convincing than the CI it justifies |
| 2026-08-19 | **Inference latency recorded from the container log: 12.9–14.5 ms** for 23–29 listing candidate sets on `Standard_DS2_v2`, the smallest instance offered | Not measuring it, or timing it client-side | Client-side timing measures the network. The deployment log is the server's own record and it came free with a screenshot that was being taken anyway. It also settles a question the project had never answered: serving this model is cheap, and the cost story is entirely about the instance-hour, not the compute |
| 2026-08-19 | **The A/B document designs "who chooses the geography — the user or the system", not "ranker vs incumbent heuristic".** Control: the user pins a neighbourhood and we rank within it. Treatment: the system ranks neighbourhoods by a demand prior, takes the top *k*, ranks within — the architecture of the Airbnb booking-intent paper §4.2 with a non-personalised prior in place of the intent model | Designing the obvious test of the shipped ranker against baseline B | Offline evaluation already answered that one as well as it can be answered (0.7530 vs 0.6429, paired +0.1139), and an online test of it would be motivated by a number that **cannot be translated into the metric it would move** — no principled NDCG-to-booking mapping exists. The geo question is the one offline evaluation *structurally* cannot answer, which is the correct division of labour between offline and online evaluation. The paper also corrected the naive reading: Airbnb does not flatten geography for large-area search, it **narrows** it per user, and our neighbourhood-scoped group is that same move without personalisation |
| 2026-08-19 | **Every quantity in the document is tagged measured / derived / hypothetical, and the tags are the deliverable.** Three of the four inputs to the power calculation are hypothetical; the traffic anchor is not | Writing a design document whose numbers all read alike | With no clicks, impressions, sessions or users, the failure mode is a reader who cannot tell which figures came from our data and which from convention. Traffic is **derived from our own reviews** — 375,806 in twelve months implies ~1,500 stays/day — with the explicit note that the second corroborating route (Inside Airbnb's occupancy estimate) is **not independent**, being reviews-based itself, so the agreement checks the parameters and not the method |
| 2026-08-19 | **`evaluate/exposure.py` measures composition and exposure, and deliberately exposes no way to compute NDCG across grouping schemes** — asserted by a test, not just a docstring | Comparing rung-1 NDCG against rung-3 NDCG to show what widening the key costs | Changing the key changes the candidate set, the ideal DCG and the random floor at once, so the two numbers share a name and nothing else. Worse, the coarsest rung **is** the grading partition, so its within-group grade distribution is fixed by construction and a model could "improve" there by doing nothing at all. Composition and reach are defined identically at every rung, which is exactly what makes them comparable |
| 2026-08-19 | **Measured: flattening to `city × room_type` asks for 23,441 listings in one request — past the endpoint's own `MAX_LISTINGS = 5000`.** The naive large-area arm cannot be served by the service we deployed | Arguing from the paper's "sheer volume" challenge in the abstract | The cap is imported from `cloud/score.py` rather than restated, so the profile and the scorer cannot drift. It turns a quoted difficulty into a project-specific fact, and it eliminates one candidate treatment arm before any test is designed |
| 2026-08-19 | **Measured: widening the key makes the cold-start deficit relatively worse, not better.** Reach against the shuffle reference runs 0.57 → 0.54 → **0.22** across the three rungs; geographic coverage of the top 10 falls to 0.69–0.73 and spread to 0.57–0.62 of the maximum | Assuming a broader candidate set would give new listings more room | The intuition is backwards: more competition for the same ten slots hurts the cohort the ranker already under-serves. This makes the cold-start guardrail **more** important in the treatment arm than the control, and it is why both it and geographic coverage are ship-blocking rather than monitored. Found before the experiment was specified, which is the point of running the offline analysis first |
| 2026-08-19 | **The sealed fold was scored for composition, and this is not a third read.** No NDCG, no baseline comparison, no paired test — only geographic spread and cohort reach | Running the exposure analysis on the development folds to leave the holdout wholly untouched | The refit model fitted the development folds, so every exposure number computed there would be optimistic; the sealed fold is the only population it did not see. The declared limit was on **performance** reads and both remain spent — reading composition neither estimates quality nor can be used to select anything. Stated in `_sealed_scores`' docstring so the next reader does not have to reconstruct the argument |
| 2026-08-19 | **The design reaches a negative verdict on itself and reports it.** At ~8 % eligibility and 4,000 sessions per arm per day, detecting the +3–5 % relative lift a ranking change plausibly produces needs **6 to 15 weeks** | Choosing an MDE that makes the run look comfortable | A design that cannot detect the effect it expects, inside a window where seasonality and co-occurring changes stay second-order, is a finding rather than a failure — and stating it is worth more than a three-week plan powered only for a win nobody expects. Four remedies are ranked by cost, CUPED first. The eligibility constraint driving it is **structural**: our scores carry no cross-group calibration, so there is no merge policy and a broad search cannot be served at all |
| 2026-08-19 | **Retracted: "the deployed endpoint cannot serve a city-wide ranking" was a circular argument and is removed.** Measured 2026-08-19: the **whole catalogue of 44,684 listings scores in 87 ms**, 23,441 in 48 ms | Ruling a design out with `MAX_LISTINGS = 5000` and the `Standard_DS2_v2` instance size | Both are **our own demonstration conventions** — a sanity bound written into the scoring script and the cheapest instance available — not properties of the problem, and using them to eliminate an experimental arm is circular. The 45 MB payload argument dissolves too: a real system sends listing ids and joins features from a store. What actually argues against flattening is model-level and already measured — geographic collapse to 0.69 coverage and cold-start reach at 0.22x chance. "Technically trivial and empirically bad" is the true statement |
| 2026-08-19 | **The experiment's arms differ at the retrieval stage, not the ranking stage.** The ranking model is byte-identical in both; only the candidate set changes | Making the ranker itself the treatment | A two-stage system hands the ranker a few hundred candidates, so "rank 23,441 listings" was a strawman — that set never reaches the model. Moving the change to retrieval isolates one variable, needs no retraining, and matches the architecture the source paper actually describes: their geo query *is* retrieval. It also makes the effect attributable, which a design changing two things at once cannot claim |
| 2026-08-19 | **Evaluate the screen, not the ranking.** First-screen composition — mean grade, relevant share, distinct neighbourhoods, cold-start share — replaces every attempt to compare rankings across candidate sets | Comparing NDCG between retrieval policies | NDCG normalises by the candidate set, so it changes meaning the instant the set changes; that is why every earlier attempt to compare grouping schemes hit a wall. **Ten listings shown is ten listings shown**, so composition is unnormalised and directly comparable — and it is also the question a guest would ask. This dissolved the problem rather than working around it |
| 2026-08-19 | **Measured, and against the predicted direction: narrowing to few neighbourhoods nearly doubles deserving cold-start exposure** (0.048 -> 0.091 at k=1) while costing 0.31 grade points of first-screen quality. At k=5 it is quality-neutral and buys nothing; k=3 is the launch configuration | Assuming a geographic filter would worsen cold start, as widening the candidate set does | A good new listing in a chosen neighbourhood competes against that neighbourhood rather than an entire city's established inventory. The prediction is registered in the design as H2 **because** it is counter-intuitive — a pre-registered surprise is worth more than a confirmed expectation. Also corrected an error of mine in the same table: narrowing **reduces** per-screen geographic diversity by construction, since the screen cannot show more neighbourhoods than retrieval kept |
| 2026-08-19 | **Searches/day is no longer presented as a derived quantity.** Duration is a lookup against eligible sessions/day; market size (~1,500 stays/day) appears only as a sanity check | Deriving ~100,000 searches/day from ~1,500 stays at an assumed 1.5 % conversion and tagging the result "derived" | A derived quantity is only as good as its weakest input, and that one was dominated by an invented conversion rate — labelling it derived borrowed credibility from the measured half. Stating sample size in sessions and letting the reader supply the traffic is both honest and more useful |
| 2026-08-19 | **The design document is written as a design document, with no reference to the project's build stages.** Status header, hypothesis, arms, metrics, sample size, analysis plan, decision rules, risks, appendices | The first draft, which was organised around the reasoning that produced it — "why this experiment and not the obvious one", "the design's own verdict on itself", "what is deliberately absent" | That is a decisions-log voice, and the decisions log already carries all of it. A reader of the design has no idea the system was built in stages and should not need to; the document describes one delivered system, retrieval and ranking together. Also added the three things a real design has and the draft lacked: a **falsifiable hypothesis**, **decision rules declared for all four outcomes**, and an **analysis plan** naming the estimator, the multiplicity treatment and the pre-registered segments |
| 2026-08-19 | **Measured, not argued: the holdout's neighbourhood gap narrows what it can speak to but does not bend what it says.** Out-of-fold, groups in the 17 neighbourhoods with no sealed listing score **−0.0075 [−0.0444, +0.0304]** against the rest — 97 groups vs 214, and the sign flips across every size band | Leaving "the sealed estimate is largely an Athens-and-Crete number" as a stated caveat | The caveat sounded fatal and was answerable. Every absent neighbourhood is in the development pool **by construction** — its geography has no sealed rows — so cross-validation already scored those groups out-of-fold, and the two sets can be compared **inside one estimate** rather than across two. The interval covers zero and is tight enough to rule out a gap above ~0.04, so the holdout is not flattered by what it excludes. It does **not** repair the per-city slices: Thessaloniki still rests on four sealed groups. Notebook 04 §11, with the size-band breakdown because the absent neighbourhoods are the large ones (median group 53 vs 24) |
| 2026-08-19 | **Recorded as a cheap miss: `assign_folds` should have carried neighbourhood presence in its strata.** It balances on city × group-size band only | Adding it now and re-splitting | It would have cost nothing and spread the ~60 small neighbourhoods far more evenly. It would **not** have placed the giant components — those must land somewhere whole — so the largest gaps would remain. Not taken because re-splitting invalidates every Phase 3 number to buy roughly two extra groups in a 72-group average, and the measurement above shows those are not the groups that would change the answer. Worth writing down precisely because the fix is cheap and the reason for skipping it is arithmetic rather than reluctance |
| 2026-08-19 | **The ex-establishment ablation is re-labelled as what it actually tests: the provenance of the label.** Notebook 04 §7 | Leaving it as a feature-importance result | The deeper problem with this target is not "availability is not bookings" — it is that **occupancy is downstream of the incumbent marketplace's own ranker**, so "orders by expected demand" and "partly reproduces Airbnb's ordering" are inseparable with one public snapshot. That is sharper than the demand-proxy caveat, which concerns what the label *means* rather than where it *came from*. The strong form is testable and fails: a model denied all 8 establishment features — the channel through which the feedback loop would arrive — still reaches **0.7031** against 0.6660 for establishment alone. Bounds the concern, does not eliminate it, and is the reason the "never write 'guests prefer'" rule exists |
| 2026-08-19 | **`report.unpaired_difference` added beside `paired_difference`, and `demo.coverage` now routes through `split.geography_coverage`** | Reusing the paired bootstrap for a slice-against-slice comparison; leaving the coverage computation duplicated in two modules | The slices share no groups, so there is no pairing to exploit and the paired interval would be too narrow — a test pins that the unpaired interval is the wider of the two on identical numbers. The coverage duplication was live: the console's missingness note and the split's representativeness report computed the same quantity in two places and could have drifted apart while both looked right |
