"""rental_ranking — search ranking for short-term rental listings.

Subpackages: data (acquisition, anonymization, filtering), features (label,
spatial, aggregates, sentiment), train (baseline, LambdaMART), evaluate (metrics).
"""
